# Program 09 execution ledger (orchestrator session 19bb4b93, started 2026-07-03 ~20:35 EDT)

Goal (Stop-hook): `project/` fully implemented = Program 09 (`project/09-program-full-game.md`) executed.
Authority: master plan §0–§4; kickoff per §3: [E:071] [W:090→091] [D:100→098-LODES] [O:096].

## Infrastructure (verified 2026-07-03 20:32)

- Worktrees under `/home/user/projects/game/babylon/worktrees/` (git-excluded via .git/info/exclude):
  - `e071` → branch `071-reactionary-subject` (Lane E)
  - `w090` → branch `090-cold-collapse` (Lane W)
  - `o096` → branch `096-observatory-foundation` (Lane O)
  - `d100` → branch `100-county-exposure` (Lane D; babylon-data repo work at /home/user/projects/game/babylon-data on its own branch)
  - All cut from dev@e3f14d02. All `mise trust`ed. Symlinks per worktree: `.venv` → main repo .venv, `data/sqlite` → trove; w090+o096 also `web/frontend/node_modules` → main.
- **GOTCHA (empirically verified)**: bare `poetry run` in a worktree imports MAIN repo babylon (editable .pth). `mise run <task>` or `PYTHONPATH=src poetry run` resolves worktree src correctly.
- Shared-resource law: Postgres 5433 canonical runs = Lane E only (O may use tick_probe short runs); web ports 8000/5173 + Playwright = Lane W only (this phase); npm/poetry installs FORBIDDEN in worktrees (shared envs).

## Lane status

- E:071 Reactionary Subject — implementer PARTIAL ~23:15 (impl DONE + verified; canonical run in flight). Branch `071-reactionary-subject` 13 commits e3f14d02..4cc3cc91, clean tree. `mise run check` GREEN 8926/0, mypy 567 files clean, income-circuit 9/9. R-PROOF: qa:e2e-regression BYTE-IDENTICAL (Δ=0.000%) → FascistFactionSystem @17.4 agitation-gated, dormant in pacified decade → NO baseline regen (ADR054). NEW: FascistFactionSystem, reactionary formulas (RLF simplex, entitlement/volatility), 4 SocialClass fields (entitlement/volatility/fascist_alignment/aligned_faction_id), 8 fascist EventType + 4 ActionType, OODA POGROM/LOCKOUT/VIGILANTISM, DecompositionSystem on-demand enforcer creation. ~52 new tests incl. induced-crisis integration (bb09d808).
  - Review wf_cd61807c-08b DONE → **REVIEW-CLEAN** (0 confirmed, 0 plausible on 217KB engine diff). Wiring-vs-dormancy REFUTED all "not wired" hypotheses (FascistFactionSystem genuinely @17.4, correctly dormant under hegemony). 4-field round-trip verified. One IMPORTANT finder finding (fascist_alignment monotonic ratchet no decay) REFUTED + orchestrator-adjudicated as DELIBERATE Constitution-I.7 quantity→quality design (ADR054 L91-93; per-tick routing reversible via solidarity in calculate_fascist_pull denom, capture STATE one-way by intent) → OWNER QUEUE as design question (should sub-threshold alignment decay in restored hegemony?).
  - **CANONICAL RUN — DONE + VERIFIED + ARCHIVED**. Session edf07b2e-ac2f-4ed7-990e-cadd159ed7b2, `--scope michigan-canada --ticks 520`. Terminal baseline tests/baselines/michigan-e2e.json: tick=519, counties_alive==counties_with_population==83, total_v=3.13e9. Archived (8 Parquet → babylon-archives, 9 partitions purged from PG). **071 PROOF WINDOW CLOSED** (byte-identical regression + canonical liveness 83/83). → E:101 unblocked on the R-PROOF axis (still needs 100 merged for S1 data).
  - POLISH PASS DONE → **spec-071 COMPLETE** (task #1). 7 minors fixed (6 commits 562edd37..80441be3, total 17 on dev..HEAD). Byte-identity HELD: qa:e2e-regression total_v Δ=0.000%, mise run check 8927/0, mypy clean. #2 (threaded ReactionaryDefines into fascist-verb resolution for III.5) confirmed byte-identical + override-honoring test. DEFER honored: entitlement_effective shipped-but-inert note (FR-015 satisfied). Accepted WITHOUT re-review (already-verified: byte-identity proof + green gate + pre-adjudicated minors). Ready for BD merge to dev. → E LANE IDLE; E:101 unblocked on proof-window axis, WAITS on D:100 re-review clean (S1 data dep) then 071+100 integration.
  - Open items (071): SPONTANEOUS_RIOT deterministic gate (good for determinism, flagged for refinement); autonomous org verb-selection deferred to Wave 2/3; aligned_faction_id 4th SocialClass field beyond catalog's 3 (review: sound necessity for capture persistence). Env: e071 worktree data/\* trove symlinks incomplete — agent recreated; "never clean:testdb during a running suite"; ~20 tri-county 1045-row test sessions + singletons accumulating hot on PG 5433 (sweep when all agents idle).
- W:091 Frontend consolidation — DISPATCHED ~21:35 (opus), same worktree w090, stacked branch `091-frontend-consolidation` off 42232a15. Carries 090 residuals (see Completed).
- D:100 County exposure — implementer DONE ~22:35. Worktree branch `100-county-exposure` (6 commits e3f14d02..94b83090): additive schema.py (2 new tables), data:exposure task, 7 schema tests. babylon-data branch `100-county-exposure` (5 commits main..HEAD): exposure/ + trade/ modules, 34 unit + 4 real-DB integration. Real-DB apply: fact_county_exposure_by_external 384,200 rows + fact_bilateral_trade_annual 120 rows; all 120 bloc-years sum 1.0; H1==H2 hash; backup dropped. Review wf_5078a298-510 DONE → FIX PASS sent to Lane D agent (reports/100-fixes.md): (1) CIRCULAR reconciliation gate (validation.py residual≡0 by construction, "≈0%" is the tell — claims "±2% vs published totals" it never computes) → real reconciliation OR honest rename+correct all artifacts; (2) SC-005 trade sum hardcoded True → compute Σmonthly==annual; (3) non-atomic pysqlite two-table swap (reproduced: mid-swap kill deadlocks recovery; blast radius confined to 2 NEW tables, trove safe) → atomic/idempotent; (4) logical_table_hash order-dependence → deterministic. ADJUDICATED correct-by-data, DOCUMENT ONLY: bloc-invariant weights + USD/tons naming. FIX PASS DONE ~later: #1 honest rename to weight_conservation (agent verified NO independent published import total in DB — only USE+TOTAL_REQ, sole import row "Noncomparable imports", empty dim_import_source) + REAL regression detector (perturb employment → weight changes); #2 verify_trade_exact_sum real; #3 atomic swap (pysqlite isolation_level=None + BEGIN, mid-swap-failure rollback test); #4 sorted-key accumulation + rounded hash, H1==H2 re-verified; #5 "Noncomparable imports" confirmed sole import measure, documented; #6/#7 spec-101 handoff: consume `total_trade_usd_millions`→`bilateral_trade_value` NOT `_tons`. worktree +2 commits (62c003a7), babylon-data +3 (6bf3024). 55 tests green, real-DB re-apply clean. FIX RE-REVIEW (agent a97f2087) IN FLIGHT.
  - **SPEC-VS-DATA FLAG (→ E:101 + owner queue)**: source fact_trade_monthly is MONETARY (USD), so loader wrote `bilateral_trade_value` NOT the program-named `bilateral_trade_tons`. 101 §2 says it populates `bilateral_trade_tons` on external-node rows — 101 agent MUST use bilateral_trade_value (USD) and rename its expectation, OR Percy decides tonnage is needed (would require a different source). Also: research.md notes dim_country blocs ≠ engine node_ids (crosswalk needed by 101) and distribution is bloc-invariant.
- O:096 Observatory foundation — implementer DONE ~21:40 (6 commits e3f14d02..4337c3d0, branch 096-observatory-foundation; 49 backend unit + 16 integration incl. live write-rejection + 17 Vitest/MSW; new web/observatory/ app, read-only sim alias + migration-refusing router, /api/observatory/\* over declared views only, flag-gated OBSERVATORY_ENABLED, React /observatory lazy route + one App.tsx line, two-DB map in HOW-TO-LOCAL-DEV.md). Review wf_1f32a0a2-392 DONE → FIX PASS sent to Lane O agent (reports/096-fixes.md): CRITICAL unbounded /hex/ dump (national res-7 = OOM; becomes 105's verification surface) → bound; IMPORTANT narrow `except OperationalError` leaks DSN under DEBUG → catch DatabaseError; IMPORTANT unbounded commits endpoint → cap; IMPORTANT seeded fixture never sparse so v_hex_state_asof carry-forward (II.11 gotcha) untested e2e → make fixture sparse + real test; minors (int overflow, sessions LIMIT, stub.py OBSERVATORY_ENABLED=True w/o sim alias, hex MSW contract table overclaim). ACCEPTED by design: urls.py root include (Django analog of the one App.tsx route line). Live-render gate still deferred to orchestrator sync (Lane W owns ports/Playwright). Prettier VerbPage/educate.json failures CONFIRMED pre-existing, not touched by O.
- O:096 FIX PASS re-review DONE → **FIX PASS CLEAN** (task #4 COMPLETE). All 4 confirmed fixes verified (hand-traced keyset pagination no off-by-one; DatabaseError catch leaks no SQL/DSN; commits bounded; sparse fixture [2,1,1,0] genuinely exercises v_hex_state_asof carry-forward — the II.11 gotcha; hex_spatial_map seeding via RW pool not the RO alias, sound). 2 LOW nits for 099 to sweep: (a) DEFAULT_HEX_LIMIT/MAX_HEX_LIMIT missing from queries.py __all__; (b) 503 path has no server-side logging. Branch 096-observatory-foundation now 11 commits (be48/be2af78c..38adfa03).
- O:099 Observatory deep panes — DISPATCHED (opus), stacked branch `099-observatory-deep-panes` off 38adfa03 in worktree o096. NOTE: real archived session edf07b2e-ac2f-4ed7-990e-cadd159ed7b2 now under BABYLON_ARCHIVE_ROOT — 099's DuckDB source=archive gate has a live target.
- Queued: W:091→092∥093→094→095→103; D:098-LODES→068-slice; O:099; E:101→102→104→105 (S1: 101 needs 100 merged; R-PROOF: one proof window at a time)

## SESSION-LIMIT RECOVERY (2026-07-04 ~00:15, model switched: Opus orchestrates, Sonnet 5 subagents per owner directive)

Account session limit hit ~midnight; O:099/D:098/E:101 agents died mid-task (W:091 fix completed clean). ALL committed progress safe in worktrees. Resumed as FRESH Sonnet agents (git worktree state = context, not agent memory).

- **INFRA LESSON (shared-Postgres contention)**: file-ownership collision law (§3) held perfectly, but ALL lanes share ONE runtime Postgres 5433/babylon_test. Concurrent integration tests deadlocked; a test momentarily truncated `immutable_reference_tiger_county` (read 0 rows mid-txn; recovered to 3222). ROOT: some Postgres integration test truncates/reloads shared immutable_reference\_\* tables without isolation. MITIGATION: stagger Postgres-heavy lanes; NEVER run E:101 canonical sim concurrently with D/O Postgres integration; rule to all agents: don't truncate immutable_reference\_\* in shared babylon_test. TODO: identify+fix the offending test (isolation bug) — assign when found.
- **Batch 1 results**: W:091 fix re-review (a5fab6a6) FIX PASS CLEAN → **W:091 COMPLETE** → W:092 dispatched. O:099 finish (add45a6a) DONE — Sonnet CAUGHT a real regression the crashed agent's docs hid: deep_queries.py imported babylon.persistence.delta.CHECKPOINT_EVERY_TICKS (engine-import-boundary violation, test_import_boundary.py; real backend was 245/1-fail not claimed 246/246) → fixed by mirroring constant locally; also corrected 25→29 integration count. O:099 code+docs done (7 commits 38adfa03..85e5ed9d; 83 unit + 29 integration + 27 vitest + 246 web; archive gate verified vs real session edf07b2e). O:099 REVIEW workflow wf_47f5a0f1-99f IN FLIGHT (Sonnet, 6 dims: readonly-safety, hash-chain-verify, live-archive-parity, sql+sparse, import-boundary, test-integrity). D:098 finish (a80b08d3) STILL RUNNING (economics pytest active on 5433 → E:101 stays held).
- **Batch 2 (HELD for after Batch 1 Postgres clears)**: E:101 resume — 3 commits landed (specify/plan/tasks, exposure map loader, national-Φ+trade attribution crosswalk); uncommitted conservation_audit.py + test_phi_week_conservation.py. Remaining: finish conservation extension, red→green trade-circuit integration test, then canonical sim:e2e-bg (Postgres-exclusive). E:101 consumes spec-100 `total_trade_usd_millions`→bilateral_trade_value (USD, not tons).

## Batch 3 (2026-07-04 ~00:55) — O:099 REVIEW ADJUDICATED + FIX DISPATCHED

O:099 review wf_47f5a0f1-99f DONE: **7 CONFIRMED, 0 refutes each** (refuters held on every one). All read-only-safe + import-boundary-clean; findings are verification-honesty + parity + silent-truncation, no sim-math. Adjudication → fix pass dispatched (Sonnet a030d9bd, worktree o096, brief `reports/099-fixes.md`, base 85e5ed9d; web=5432 so runs parallel to 5433 lanes):

1. created_at_utc nulled for source=archive (views.py:405) — Parquet HAS it; add to COMMIT_CHAIN_SQL, stop nulling → CLEAN FIX.
1. boundary/conservation cap 5000 rows, no truncated signal (deep_queries.py:239) — late-tick ALARM rows silently dropped → add truncated flag (hex-frame pattern) + UI indicator.
1. hex/ endpoint ignores ?source= entirely (views.py:425) — violates FR-001 + contract; silently serves empty live data for purged-partition archive sessions → implement archive read (DuckDB as-of over sparse Parquet, II.11) OR explicit-reject + owner note. No silent ignore.
   4/5/6. hash-chain verify only checks len==64, never content (deep_queries.py:81) — 3 dims same defect; "CHAIN VALID" badge overpromises. NOT a plan-conflict: FR-008 forbids re-running the ENGINE, not recomputing the hash. Fix = INVESTIGATE-THEN-FIX: ground-truth the real determinism_hash write path (two finders cited DIFFERENT formulas: conservation_audit.compute_determinism_hash over hex payload vs runner sha256(session:tick:seed)); if recomputable from session-meta+row w/o engine re-run → genuine recompute+compare; else honest relabel (structural-only) + owner-queue note (true content verify needs schema change or engine re-run). + add content-tamper test (test_verify.py — currently only length mismatch tested).

## E:101 Trade activation — PARTIAL → REVIEW IN FLIGHT (2026-07-04 ~01:05)

E:101 agent (ad4a37cb) DONE returning **PARTIAL**. Branch `101-trade-activation` **pushed to origin**. Topology: 071 + merge of 100 (at 8cea433b) + 7 own commits (5355adfe..eb60d8f2). Own-work review base = **8cea433b** (isolates 101 from already-reviewed 071/100).

- **LOAD-BEARING DISCOVERY**: Hickel drain is a SINGLE national aggregate → naive wiring left every bloc Φ=0. Agent added national-Φ→bloc **attribution crosswalk** (`_NODE_TO_BLOC`, injective node→bloc, split by bilateral-trade share; commit f1183402, spec.md D3/ADR055). Also: conservation invariant Σ DRAIN_EDGE ≡ Φ_week per bloc (51289ec0), TickContext wiring (c3cab3b4 — Φ distributes every tick), county-exposure map loader (08f5f47e, scope-renormalised), 5-tick re-baseline + written proof (e059e50e).
- Gates (agent-claimed, tri-county): 18 unit + test_trade_circuit.py (4 GREEN, RED-verified w/ wiring reverted) + test_bridge_income_circuit (9); qa:e2e-regression total_v Δ=0.000% (byte-identical); conservation residuals ~1e-16, zero --strict alarms. Consumes spec-100 `total_trade_usd_millions`→bilateral_trade_value (USD ✓ not tons).
- **CANONICAL RUN LIVE**: PID 1800388, session `a8202ed0-f157-4171-9d10-9624733ac12d` (michigan-canada 520t). Verified healthy tick 218/520, 83 counties, ~1661 kB/tick. Post-completion: verify 83/83 + commit michigan-e2e.json terminal baseline + sim:archive. (Agent's 1st launch died on ix_hex_state_session_tick contention; 2nd is this healthy one.)
- **REVIEW dispatched**: wf_d183f7a6-020 (Sonnet, 6 dims: phi-attribution-correctness, conservation-invariant-integrity, wiring-and-determinism, baseline-honesty, data-grounding-and-scope, boundary-and-frozen-models; 2 refuters each). READ-ONLY constraint enforced (canonical run on 5433 — reviewers inspect committed report.json/baselines statically, NO pytest against Postgres). Base 8cea433b..eb60d8f2 (137KB pkg).
- **OWNER QUEUE**: ratify/adjust `_NODE_TO_BLOC` Φ-attribution crosswalk (india/latin_america→Φ=0; russia_csi→Europe weak). 101+102 proof window stays OPEN.

## Completed

- **W:091 Frontend consolidation** — COMPLETE (task #5). Fix pass re-review (Sonnet a5fab6a6) FIX PASS CLEAN: ErrorBoundary around Briefing map (real fallback + throw test), 5 orphans deleted (tsc clean, zero dangling refs — VerbShell confirmed never-tested via git history), 2 e2e god-page relics deleted (genuinely dead UI), honest close-out (Vitest 364, owner-verification-pending gate, 042 line counts fixed). tsc clean, leaflet empty, Vitest 364/364, backend 248. 18 commits. Owner-run Playwright checklist for Percy in reports/091.md. Ready for BD.

- **W:092 Event Log + Tick Resolution** — DISPATCHED (Sonnet a790f162), stacked branch `092-event-log` off c1d1a834 in worktree w090. W uses PRODUCT Postgres 5432 (no contention w/ 5433 lanes). 092→093 SERIALIZED (both touch engine_bridge.py).

- **D:100 County-exposure loader** — COMPLETE (task #3). Fix pass re-review (agent a97f2087) FIX PASS CLEAN: honest weight_conservation rename complete across all live artifacts (verified NO independent published import total in DB), real regression detector + golden hand-computation, atomic pysqlite swap with genuine crash-between-tables rollback test, order-independent hash (permuted input → identical), BEA "Noncomparable imports" documented as sole import measure, spec-101 handoff unambiguous (consume total_trade_usd_millions→bilateral_trade_value, NOT \_tons). 55 tests green. worktree 100-county-exposure + babylon-data 100-county-exposure both ready for BD. 2 trivial cosmetic notes (residual "reconciliation" in planning artifacts; imprecise dry-run docstring) — non-blocking.

- **E:071 Reactionary Subject** — COMPLETE (task #1). Review-clean + proof-closed + polished (17 commits, byte-identical Δ=0.000%, canonical 83/83 archived). See E lane above. Ready for BD.

- **O:096 Observatory foundation** — COMPLETE (task #4). Fix pass re-review FIX PASS CLEAN. See O lane above.

- **W:090 Cold Collapse** — complete (commits e3f14d02..42232a15, review clean after adjudication). Implementer 357/357 web:check, red→green token contract 42/42, 4 OFL fonts self-hosted, amendment drafted, constitution untouched. Review workflow wf_1706053c-44c (15 agents): CONFIRMED 1 (CLAUDE.md speckit append outside W ownership → ACCEPTED by design, program-mandated tooling, trivial merge conflicts expected across all lanes); PLAUSIBLE 1 (rent ramp non-monotonic terminal → canon-faithful, carved out in amendment §66-67, Percy adjudicates at ratification); REFUTED 3 (heat/biocapacity ramps + monotonicity-untested — spec's research R3 + data-model explicitly except alarm terminals/diverging). Adjudicated: literal `rg -i inter` gate unsatisfiable vs canon (`--interactive-*` matches) — intent verified via `rg -iw inter` empty.

- 090 residuals assigned to 091: prettier 3.1.0(hook)/3.8.1(gate) mismatch; CREATE Playwright visual-baseline suite (none exist in repo); Space Grotesk no italic face (faux-italic in BreakdownTooltip); 35 canon semantic type-role tokens unported without provenance note; C6 lens→layer read-back tautology; colors.ts docstrings say "luminance-monotonic" unqualified (align wording with amendment exception).

## D:098 slice — PREMISE CORRECTED (2026-07-03 late)

Agent returned NEEDS_CONTEXT with a verified diagnosis: the 098-LODES task premise (finish loader_od.py to hydrate immutable_reference_lodes_od_matrix; ~34 NoDataSentinel = "LODES absent") is FALSE. Reality: (1) LODES present — fact_lodes_commuter_flow 2.6M rows 2010–2021; (2) immutable_reference_lodes_od_matrix is a Postgres runtime table already hydrated by spec-063 LODESCommuteMatrixLoader (has contract tests); the named loader_od.py is a different county-level loader for the already-full fact_lodes_commuter_flow; (3) the failures = 21 FAILED (QCEW-adapter schema drift post-086: economics/throughput/adapters.py reads own_code='0' + 2-digit sectors from fact_qcew_annual which is now 6-digit-leaf-only, total moved to fact_qcew_county_rollup — both return NoDataSentinel) + 11 SKIPPED (test hardcodes TEST_YEAR=2022 > LODES max 2021). ORCHESTRATOR VERIFIED collision-safety: n_calculator/ThroughputCalculator UNWIRED in headless canonical runner + no engine system consumes it → adapter fix BASELINE-NEUTRAL; adapters.py DISJOINT from E:101 files → no proof-window collision. AUTHORIZED real fix (task #12 re-scoped, branch 098-qcew-adapter-fix off 100). CORRECTS stale project/01 + memory note ("LODES absent" → QCEW-adapter drift). National-scope note for D:068+E:105: hex-matrix loader is study-area-pruned/disk-driven — national is a config expansion, not a loader gap.

## Owner-review queue (present to Percy, do not merge)

- Article VII Cold Collapse amendment (from 090, R-VII) — DRAFTED at specs/090-cold-collapse/article-vii-amendment.md (proposed Amendment M). Includes the alarm-terminal luminance exception (§66-67) — ratifying it resolves the rent/heat ramp vs "monotonic binds absolutely" tension in 09 §1 R-CRT.
- 090 PR note: program gate `rg -i 'roboto mono|inter' index.css` is unsatisfiable for a faithful canon port (`--interactive-*`, `pointer-events`); font removal verified via word-boundary search instead.
- E:071 fascist_alignment ratchet — DESIGN QUESTION: alignment is monotonic (no decay) by deliberate Constitution-I.7 quantity→quality design (per-tick routing IS reversible via solidarity; captured STATE is one-way by intent, ADR054 L91-93). Should sub-threshold alignment decay when hegemony is restored? Adjudicated deliberate for now; owner confirms.
- E:101 `_NODE_TO_BLOC` Φ-attribution crosswalk — DESIGN DECISION: national Φ (single Hickel aggregate) is split to blocs by bilateral-trade share via an injective node→bloc dict (spec.md D3/ADR055). india/latin_america currently → Φ=0; russia_csi→Europe is a weak mapping. Ratify or adjust the crosswalk. (Isolated dict; changing it re-runs the 101 proof window.)
- E:101 scope-renorm drain magnitude — DESIGN/DATA: sub-national scoped runs (michigan-canada) absorb the FULL national bloc Φ_week (not a proportional share), so the Σ DRAIN_EDGE ≡ Φ_week invariant is tautological at sub-scope and drain magnitude is ~100-200× receiving-county output. Whether national scope is required for meaningful magnitudes = the E:104/105 national axis. Owner decides.
- O:099 hash-chain verification is STRUCTURAL-ONLY — GROUND TRUTH: tick_commit.determinism_hash = sha256(session_id:tick:seed), seed=0 (DDL default, not stored per-session) → a pure identity/format hash with zero world-state content; the genuine content hash (conservation_audit.compute_determinism_hash) writes to conservation_audit_log which the pane never reads. Relabeled honestly ("STRUCTURE OK"). True content/tamper verification would need a tick_commit schema change (persist hash inputs) OR an engine re-run. Owner decides if that's wanted.
- O:099 hex/ endpoint rejects source=archive with 501 — archived dynamic_hex_state spatial keys (county_fips/state_fips/region_id) are 100% NULL because hex_spatial_map (reference data) isn't in archival.EXPORT_TABLES; faithful per-hex archive reconstruction is impossible from Parquet alone. To enable it: export hex_spatial_map per-session OR add a reference-data join path. Owner decides.
- D:098 Oakland "bedroom community" empirical conflict — 3 throughput tests assume Oakland is a net job EXPORTER, but real LODES data (all 3 years) shows it's a net IMPORTER (residence 554,099 < workplace 696,243). Left failing with KNOWN CONFLICT docs. Owner call: correct the test assertions to match data, or investigate the LODES source.
- W:093 balkanization-seed DATA gap — the map political lenses (stance/faction/claims) are built + wired correctly over the API, but NO scenario seeds spec-070 balkanization graph data into the product game (seed_influences.json + its producing pipeline were never built), so a live seeded game shows honest "no data" on those lenses. Affects "game works locally" (a player sees empty political lenses). Owner call: is seeding spec-070 balkanization into the product game in-scope for this milestone (→ a new DATA-lane task), or acceptable as "no data" for now?
- E:101 hex_spatial_map robustness — engineering task #18 (not an owner decision), noted here for visibility: the recurring shared-DB contention root (global non-session-scoped spatial table + silent-zero terminal query) must be hardened before E:105 national.
- ⚠️ SYSTEMIC (two independent specs): the III.7 GATE-1 "determinism hash" does NOT verify cross-run reproducibility. O:099 found tick_commit.determinism_hash = contentless sha256(session_id:tick:seed) identity digest (seed=0 unstored); E:102 independently found BOTH tick_commit AND conservation_audit_log determinism-hash embed session_id → always diverge cross-run. So the hash-chain cannot detect content tampering NOR confirm same-config-same-output. Real cross-run determinism must be checked by comparing persisted values (v_hex_state_asof + DRAIN_EDGE), which E:102's test now does. Owner decision: is a genuine content/determinism hash wanted (needs schema change — persist hash inputs session-independently), or is value-comparison sufficient? Affects the Observatory "verify" pane framing + any determinism-gate claims.
- E:102 gamma hydration is shipped-but-INERT — get_gamma_basket hydrates from real data but its sole caller (TickDynamicsSystem) is unwired in the headless runner, so the sim never consumes it. Owner decision: wire melt/basket_calculator into the canonical runner (would change the baseline — needs a proof window), or leave gamma inert until a later spec needs it? Same shipped-but-inert pattern as D:098 n_calculator + 071 entitlement_effective.

## Wave 4 (2026-07-04 ~01:35) — canonical COMPLETE + E:101/W:092 reviews landed

- **E:101 CANONICAL RUN COMPLETE** (not a crash — "DEAD" = finished): session `a8202ed0-f157-4171-9d10-9624733ac12d` ran clean through tick 519, consciousness/demographics/employment=83 (83/83 liveness ✓), auto-refreshed `tests/baselines/michigan-e2e.json` (Spec-065 T085) — currently UNCOMMITTED (`M`) in e101. Needs: gated-field verify vs 071 baseline → commit → sim:archive. (Handled by the E:101 fix agent, finding #7.)
- **E:101 REVIEW wf_d183f7a6 DONE: 8 CONFIRMED, 1 PLAUSIBLE, 9 minors, 2 refuted.** Core SOUND (Φ genuinely wired every tick; total_v byte-identical by redistribution — a minor explicitly confirms this). Confirmed defects = robustness/honesty (same "gate can't fail / silent-zero" class as this impl's 100 review): (#1) \_attribute_phi_and_trade silently drops 100% Φ when no bloc has trade (postgres_initialization.py:377) — fail-loud; (#2) Φ silently zero for start_year outside Hickel 1980-2017 (398) — preflight guard; (#3/#4) Σ DRAIN≡Φ_week tautological + all-zeros passes silently (conservation_audit.py:170-172) — add non-zero/coverage guard that actually catches a zeroed dist; (#5) external_node_flows keyed by FIPS not bloc, contradicts ADR055 (run_summary.py:144) — reconcile doc/code; (#6) test_conservation_identity_holds_per_bloc vacuous, no non-emptiness guard (test_trade_circuit.py:132) — weakens RED→GREEN; (#1/#8 dup) proof.md LAUNDERS inherited 070/071/dialectics drift (max_tension 1.0→0.667, p_acquiescence homogenized across demographically-distinct counties, event-composition shift) as spec-101's — 106 commits since spec-086 last regen this baseline; NONE of 101's commits touch Survival/Contradiction/Lifecycle/event-emission → honest-disclosure rewrite. FIX dispatched (Sonnet a7394d7e, e101, 5433-free) — brief reports/101-fixes.md; HARD byte-identity constraint on happy path (guards are no-ops when data present). NOTE the p_acquiescence homogenization is a possible LATENT SurvivalSystem bug (predates 101; read-only review couldn't run sim to confirm root cause) — investigate separately, NOT a 101 blocker.
- **W:092 Event Log — DONE** (Sonnet a790f162). Branch `092-event-log` 4 commits c1d1a834..fa5815b4 (stacks on 091). Backend: resolve_tick persists to tick_event (\_persist_tick_events_safe), query_session_events, journal/alerts dashboards over REAL history (were {}). Frontend: EventLogPage replaces /log stub, new TickResolutionPage /resolution, End Turn on OrgsPage (resolveTick had 0 callers). Counts: backend tests/unit/web 255/255 (+7), persistence 360/360, Vitest 378/378 (+14), web:check green; Playwright end-turn-flow owner-run (gated). One pre-approved persistence file (postgres_runtime/\_legacy.py, additive). Disclosed open item: eventClassifier.ts UPPERCASE-key vs lowercase-enum → real events default "informational" (claimed pre-existing/out-of-scope). REVIEW dispatched wf_d5b4db0b (Sonnet, read-only, 5 dims incl. classifier-honesty verification). 093 serialized after 092.
- **D:098 — 3 clean commits, NOT yet accepted.** Branch `098-qcew-adapter-fix` (62c003a7..a0fa6fb2): core adapter fix (4053b653 — post-086 rollup + sector-aggregate leaves), LODES-year-pin to 2021 (fecfe1f8), project/01 correction (a0fa6fb2). adapters.py reads SQLite reference DB (NOT 5433). Agent returned muddy ("Monitor bed8gfb7a armed" — backgrounded a test:unit run, no counts). Orchestrator scoped-verify (43 tests) TIMED OUT at 300s (~41%, **2 F visible**) — clean re-run bscwfel1b in flight for failure names. HOLDING accept until failure list known (could be residual QCEW OR unrelated pre-existing).
- ~~Still running: O:099 fix a030d9bd (5432).~~ → **O:099 COMPLETE** (task #11, see Wave 6).

## Wave 5 (2026-07-04 ~01:50) — D:098 DONE (honest) → review dispatched

- **D:098 DONE with full evidence** (agent resumed when its Monitor bed8gfb7a fired on the backgrounded test:unit run). **21 QCEW schema-drift NoDataSentinel failures → 0** (Wayne 2022 total verified = 714,597). Fix matched diagnosis: total ← fact_qcew_county_rollup; sectors ← aggregate 6-digit leaves via dim_industry.sector_code; ALSO fixed get_county_naics_wages → Σwages/Σemployment (robust to spec-086 nulling per-leaf avg_weekly_wage). New unit test claims real \_sector_codes_for semantics not tautologies. check:quick green, 8856 unit pass. Branch 098-qcew-adapter-fix (62c003a7..a0fa6fb2, 3 commits).
- **6 remaining failures — NOT the schema bug, honestly categorized**: 3× test_detroit_wiring.py (pre-existing MELT-unavailable-2022, identical on base) + 3× Oakland "bedroom community" — NEW DISCOVERY via un-skip: Oakland is a net job IMPORTER in all 3 LODES years (residence 554,099 < workplace 696,243), contradicting the test premise. Agent left them FAILING with inline KNOWN CONFLICT docs + project/01 flag — NOT silently flipped. → OWNER QUEUE. Also: only test:unit failures = pre-existing flaky-Postgres TestRunSweep deadlocks (identical on base, nondeterministic).
- **REVIEW dispatched** wf_098 (Sonnet, 3 focused dims: sector-aggregation-correctness [double-count/missing-leaf/ownership-drift — the core risk], wage+test-integrity [div-by-zero, weakened-to-pass assertions], honest-accounting+scope). Read-only + AUTHORIZED targeted SQLite queries vs reference DB (sharpest aggregation check). Scrutiny justified by this impl's D:100/E:101 tautology+silent-drop pattern.
- Orchestrator cross-check (bscwfel1b, 844s) CONFIRMS D:098 honesty independently: test_throughput_validation.py + test_adapters_sector_expansion.py = **40 passed / 3 failed**, the 3 being exactly the Oakland conflict tests (net balance +166,150 → is_job_importer=True), ZERO QCEW NoDataSentinel remaining. The other 3 (detroit-wiring MELT-2022) live in test_detroit_wiring.py (not in my scoped run) = the agent's 6-total. Basic honesty verified; formal aggregation-correctness verdict pending wf_382ae085.

## Wave 6 (2026-07-04 ~02:05) — O:099 COMPLETE → **O LANE DONE**

- **O:099 fix (a030d9bd) DONE — VERIFIED CLEAN → task #11 COMPLETE.** 3 commits 85e5ed9d..c19a4a87. All 7 confirmed findings resolved: #4 archive commits carry real created_at_utc; #6 boundary/conservation truncation indicators rendered; #5 hex/ explicit-501 (empirically justified — archive spatial keys 100% NULL, hex_spatial_map not exported); #1/#2/#7 hash HONEST RELABEL Branch B (ground truth: determinism_hash=sha256(session:tick:seed), seed=0 unstored → pure format hash, content-verify impossible from pane data; badge "CHAIN VALID"→"STRUCTURE OK", verification_scope:"structural" field + honest docstrings/contract/spec); #3 content-tamper test added (non-vacuous — flips a byte, asserts stays valid + scope=structural, LOCKS IN the honest limitation). Orchestrator self-verified (not full re-review, justified: green tests + authorized-decision match + empirical backing): `rg CHAIN VALID` clean (only a removal-locking test + module comment), badge relabeled, tamper test genuine. Tests: 124/124 backend + 339/339 vitest, tsc/eslint clean; web:check red only on pre-existing prettier VerbPage.tsx/educate.json (outside O ownership, untouched). 2 owner-queue items added (hash content-verify schema/re-run; hex archive export). Ready for BD.
- **O LANE COMPLETE**: O:096 ✅ + O:099 ✅ = the full Observatory lane (Program 09 §3 O[096→099]) done, pending BD merge. First fully-closed lane.

## Wave 7 (2026-07-04 ~02:30) — W:092 + D:098 reviews landed → both fix passes dispatched

- **W:092 REVIEW wf_d5b4db0b: 10 CONFIRMED (4 critical), 2 plausible, 4 minors** — a bug 255 green unit tests all missed (they mock persist_tick_events). Root defects: (A CRITICAL) tick_event.severity VARCHAR(12) can't hold 13-char "informational" default (fallback for ~56/70 EventTypes) → real Postgres StringDataRightTruncation → whole tick batch rolls back → bare-except swallows → resolve_tick 200 OK with ZERO events persisted; the never-done spec-061 T047 column-widening. (B CRITICAL×3) EventLogPage + TickResolutionPage DISCARD backend's correct event.severity and re-derive via broken client eventClassifier (UPPERCASE keys vs lowercase enum) → severity filter + "ascending drama" staging non-functional on real data (every event → OBSERVED/informational); fix = USE backend severity directly (deletes re-derivation), reconcile FE `important` vs BE `warning` vocab. (C CRITICAL) OrgsPage End Turn navigates even when resolveTick FAILS. (D IMPORTANT) no real-Postgres round-trip test — the gap that let A ship. + idempotency (dup tick_event rows on re-resolve), bare-except masks data loss, .gitignore no-op (6 tracked Playwright artifacts). FIX dispatched (Sonnet a270a889, w090, 5432) — brief reports/092-fixes.md. NOTE: agent's disclosed "classifier open item" was a MISFRAME — real fix is use-backend-severity, making fix-vs-document-classifier moot.
- **D:098 REVIEW wf_382ae085: 4 CONFIRMED (all important), 0 refuted, 6 minors.** Aggregation CORE verified SOUND (minors: no double-count [6-digit-leaf-only, PK], no ownership drift [own_code='5' Private before+after; only total→rollup own_code='0'], Wayne 714,597 reproduces, no NULL-leaf drop, sector≠total by-design). Real defects: (1 IMPORTANT) 0-vs-None conflation — get_county_naics_employment:314 + get_county_employment_by_naics:364 truthy checks (`if total else None`/`if emp:`) turn real SUM=0 into None/dropped (1,072 zero combos for sector 21 alone, live-verified fips=01011/21/2024→None); reverses pre-fix `is not None`, violates data_sources contract, inconsistent w/ file's own siblings → restore is-not-None. (2 IMPORTANT) project/01 mischaracterizes the 3 detroit-wiring failures as "unrelated MELT-2022" — they're the SAME spec-086 QCEW-drift bug in an UNPATCHED SIBLING adapter (MELT path) → correct doc + assess/fix sibling. FIX dispatched (Sonnet a23632159, d100, SQLite) — brief reports/098-fixes.md. (My earlier praise of the agent's "honest accounting" was itself off on the detroit-wiring categorization — the review corrected it.)
- **3 fix passes now in flight** (all parallel-safe, distinct DBs/worktrees): E:101 fix a7394d7e (5433), W:092 fix a270a889 (5432), D:098 fix a23632159 (SQLite).
- **D:098 FIX LANDED + core verified** (a23632159 yielded on its slow backgrounded regression suite — will resume w/ full report). 2 commits a0fa6fb2..2cb081b1: 98182721 restores 0-vs-None (line 320 `if total is not None`, 372 `if emp is not None` — matches sibling convention); 2cb081b1 took the PREFERRED path on #2 — actually PATCHED the sibling MELT national-employment adapter (should clear the 3 detroit-wiring failures too). Orchestrator verified core fix directly (reviewer's repro): get_county_naics_employment('01011','21',2024)=0 (was None), sector 21 in dict=0 → PASS. **D:098 COMPLETE (task #12).** Agent resumed DONE: 221 passed / 3 failed (3 = owner-queued Oakland only). Fix #1 0-vs-None RED→GREEN. Fix #2 PREFERRED path: patched sibling SQLiteQCEWNationalEmploymentSource.get_national_employment (melt/adapters.py — queried own_code='0'+naics_code='10' → 0 rows post-086 → NoDataSentinel every year → "MELT unavailable 2022" + "No tick dynamics snapshots"); now reads fact_qcew_county_rollup + SUMs counties → all 6 detroit-wiring tests pass, national 2022=146,158,953. Orchestrator ruled out double-count: 3220 counties = 3220 rows, SUM=146,158,953 (clean). ruff/mypy strict clean, baseline-neutral (MELT unwired). Ready for BD. → **D:068 (BEA national I-O completion slice) UNBLOCKED on the 098 axis.**

## Wave 8 (2026-07-04 ~02:35) — D:068 SEQUENCING FINDING → DEFERRED into E proof-window

Verified D:068 ground truth (tasks.md checkboxes are STALE — 0/79 checked — but master plan's "~60/77 done" is ACCURATE): bea/ package + tools/load_bea_io.py exist; fact_bea_national_industry=1065 rows, fact_bea_io_coefficient=131,239 rows; git shows spec-068 Phases 1-6 landed (US1/US2 done, US3 PARTIAL = BEAShareLookupService T050-52 landed, US4 done). **The deferred remainder is the US3 hex_hydrator wiring T056-T058 — the master-plan GATE — and it is NOT a pure data task:**

- **T056** replaces `_INTERMEDIATE_INPUTS_FRACTION = 0.5` (hex_hydrator.py:107, still inert today) with per-county BEA lookups. **CHANGES THE CANONICAL BASELINE** — SC-005 (T047) literally requires terminal stddev(c/v) ≥ 0.2 (heterogeneous county c/v is the deliverable). Baseline-changing by design.
- **T057** modifies `src/babylon/engine/factories.py` (+ conftest) — **ENGINE files (E-lane ownership per §3; D lane explicitly does NOT own engine systems).**
- **DECISION**: D:068's hydrator wiring is a baseline-changing engine-dynamics change → collides with §3 file-ownership + R-PROOF one-window discipline while E:101/102's proof window is OPEN. **DEFERRED into the E-lane sequence** — slot it just before/with E:104 (national tick-compute), since "serves --scope=national without fallback" is exactly the E:104/105 national prerequisite (master plan deps line 531 lists 068-completion as a dep of the national spec). NOT dispatched as a concurrent D task.
- Data-only remainder (T064 Shaikh CLI, T065 --rollback, T066 --dry-run, tasks.md check-off) is D-lane-safe but low-value alone → bundle it with the hydrator slice when D:068 is done properly, rather than fragment.
- **NET: D lane's independent work (100+098) is COMPLETE; its remaining piece (068 hydrator) is E-proof-window-gated. Active fronts now = E (101 fix→102→[068 hydrator]→104→105) + W (092 fix→093→094→095→103).**

## Wave 9 (2026-07-04 ~02:40) — W:092 COMPLETE → W:093 DISPATCHED (first 2nd-wave spec)

- **W:092 fix (a270a889) DONE — VERIFIED CLEAN → task #6 COMPLETE.** 5 commits fa5815b4..8044d7c5, every defect RED→GREEN: (A/D) migration 0031 widens tick_event.severity VARCHAR(12)→(32) — agent EMPIRICALLY confirmed the landmine live on product 5432 (`\d tick_event` showed VARCHAR(12)), new real-Postgres integration test RED (StringDataRightTruncation) → GREEN; (B) EventLogPage+TickResolutionPage now read event.severity DIRECTLY (orchestrator verified: classifyEvent GONE from both, filter/color by e.severity), aligned to backend union critical/warning/informational, classifier untouched for legacy tray; (C) End Turn checks store.error post-await before navigating (resolveTick never throws — store convention); idempotency DELETE-before-INSERT per (game_id,tick) verified 2→1 rows; bare-except finding verified ALREADY-handled (logger.exception) = false positive, no change; minors (gitignore path + id-mismatch doc). Gates: 380/380 Vitest (+2), 618 pytest, 3 integration vs live 5432; tsc/eslint/prettier clean. Orchestrator self-verified (green gates + authorized-decision match + empirical live-DB confirmation of the critical bug → no full re-review). Owner-queue: eventClassifier UPPERCASE tray casing (scoped to small map-recast, deferred); journal-id UUID5 mismatch (latent, needs new column if id-stability becomes load-bearing). Ready for BD.
- **W:093 Territory/Org Detail + map lens set — DISPATCHED** (Sonnet acfb38c9, FRESH ~2-sprint spec, greenfield speckit). Branch `093-territory-org-detail` off 092@8044d7c5 in w090. Scope (project/09 L227-252): TerritoryDetail+OrgDetail screens (mockups), themap/ lens upgrade (state outlines/influence rings/heat/CLAIMS hulls/stance encoding/lens modes/Collapse-Moment over LIVE spec-070 data), BreakdownTooltip provenance everywhere, get_economy + econ panel, DE-FIXTURE the 5 Wayne-fixture verb-target endpoints (get_educate/aid/mobilize/attack/reproduce_targets) with real queries, state→BEA-EA→county LOD at Michigan scope. VIII.9 binds hyperedge rendering (choropleth/badges/UpSet, NEVER pairwise fans/hyperedge-hulls; geographic CLAIMS hulls OK). Gate: Playwright lens-cycling + get_economy contract + `rg 26163 engine_bridge.py` fixture-clean. RISK flagged to agent: de-fixture needs real territory/econ data in product 5432 — report NEEDS_CONTEXT if seed lacks it (don't fabricate).
- **Active streams: E:101 fix a7394d7e (5433) + W:093 impl acfb38c9 (5432).** O lane done; D independent work done.

## Wave 10 (2026-07-04 ~02:50) — ⚠️ E:101 michigan-e2e.json AUTO-REFRESH IS ZEROED — do NOT accept

**CRITICAL / recovery-critical:** E:101 fix (a7394d7e) landed 4 GOOD code-fix commits (eb60d8f2..a66faba3: #1 fail-loud zero-denom 01f93080; #3/#4 genuine conservation detector + vacuous-test guard 0926f86c; #5 external_node_flows re-keyed to bloc f02aebb3; #6/#8 honest drift disclosure + scope-renorm owner-queue a66faba3). BUT the auto-refreshed **tests/baselines/michigan-e2e.json (uncommitted `M`) is ZEROED**: terminal_state.total_v=**0.0**, counties_alive=**0**, counties_with_population=**0** at tick 519 — vs 071's committed baseline total_v=**3126580386.69**, 83, 83. This is finding #7's BLOCKED trigger.

- **NOT a dynamics regression — a SERIALIZATION bug**: e2e.log for session a8202ed0 shows "persist_tick tick=519 consciousness=83 demographics=83 employment=83" (83 counties persisted at terminal), and tri-county qa:e2e-regression was byte-identical. Data is there; the michigan-e2e.json serialization wrote zeros.
- **PRIME SUSPECT: spec-101's own run_summary.py terminal_state path** (the baseline is built by run_summary.py, which spec-101 EXTENDED — the `external_node_flows` key is 101's addition; f02aebb3 re-keyed it again). 071's canonical run — same michigan-canada 520t, tick 519, post-spec-089 sparse — produced a VALID baseline, so the path worked BEFORE 101 → likely 101's OWN regression (in scope). COMPETING: II.11 sparse-hex raw read at MAX(tick) (would need v_hex_state_asof).
- **INTERVENTION**: 2 messages to agent — (1) DO NOT commit the zeroed baseline (finding #7 BLOCKED); confirm via v_hex_state_asof at last committed tick (tick_commit) whether data is intact (~83/3.13e9=serialization bug) vs collapsed (=real regression, escalate); (2) rebalanced attribution — check run_summary.py terminal_state aggregation (county-liveness/total_v) for a spec-101-introduced break BEFORE claiming pre-existing; fix the serialization (E-owned), regenerate baseline from the still-live a8202ed0 session (don't re-run 520t if targeted re-serialize works), commit only when total_v≈3.13e9/83-83. **HOLD sim:archive of a8202ed0** (purges the session; need it live to re-serialize). Keep the 4 code fixes.
- **FUTURE-SELF: do NOT accept/commit a zeroed michigan-e2e.json. E:101 is NOT complete until the baseline reproduces 83/83 + total_v≈3.13e9 (or the agent proves a legitimate reason it changed).** This also implicates the E:105 national-canonical-acceptance gate (needs a valid baseline-refresh path). Agent investigating.

## Wave 11 (2026-07-04 ~03:05) — E:101 COMPLETE (baseline resolved, root cause airtight) → E LANE 101 CLOSED

- **E:101 fix (a7394d7e) DONE_WITH_CONCERNS → task #14 COMPLETE.** 5 commits eb60d8f2..8210db17. All 8 findings fixed: #1 fail-loud zero-denom, #2/#3/#4 GENUINE conservation detector + non-vacuous test (RED verified: wiring reverted → "no DRAIN_EDGE rows... boundary rows must exist for positive-Φ blocs [canada,china,eu,russia_csi,southeast_asia,sub_saharan_africa]" HARD FAIL; #3 all-zero → alarm), #5 external_node_flows re-keyed to bloc (resolves kind='external' side, fixes GROUP BY dest_node_id county-keying + stale flow_type literals trade_inbound→trade_edge/commute_outbound→commute_out), #6/#8 honest drift disclosure. Gates: **qa:e2e-regression total_v Δ=0.000%**, 38 trade tests pass, **mise check 8959/0**.
- **BASELINE RESOLVED + COMMITTED (8210db17) + orchestrator-verified**: michigan-e2e.json now total_v=3126580386.69231 (vs 071 ...692311, Δ=1e-6 float noise), 83/83, tick 519. So spec-101 is byte-identical at CANONICAL scope too (Φ redistribution conservation-preserving).
- **ROOT CAUSE OF THE ZEROING — airtight, corrects BOTH my hypotheses**: NOT spec-101 (terminal_state path `_query_terminal_aggregates`/`_county_terminal_snapshot` is untouched spec-065 code; 101's run_summary change is `aggregate_external_node_flows` ONLY), NOT a dynamics regression (v_hex_state_asof at MAX(tick_commit)=519 → 83 counties/3.126e9; raw dynamic_hex_state WHERE tick=519 = 0 rows = the II.11 sparse gotcha). **Actual cause**: all 455,720 hex rows have inline county_fips=NULL (spec-088 S3) → county resolution depends on the GLOBAL non-session-scoped `hex_spatial_map` (derived from TIGER), which a CONCURRENT LANE wiped during the documented shared-DB contention → COALESCE(NULL,NULL)→scope filter excludes every county→zeros. Rebuilt from the still-live session after contention cleared.
- **CANONICAL SESSION ARCHIVED**: a8202ed0 → 10 Parquet files @ babylon-archives, 9 partitions purged (shared immutable_reference\_\* + contradiction_field 2595 correctly retained). E:101 fully closed, ready for BD.
- **⚠️ NEW HIGH-PRIORITY LATENT ROBUSTNESS FINDING (the recurring-contention ROOT, out of 101 scope)**: `hex_spatial_map` is a GLOBAL non-session-scoped table (spec-088 S3) → a concurrent 5433 session/test can transiently corrupt ANY run's county spatial resolution; AND the terminal-aggregate query SILENTLY ZEROS (COALESCE→excluded) instead of failing loud → nearly shipped a garbage canonical baseline. This is the same root as the ledger's "immutable_reference_tiger_county truncation" contention. **Fix before E:105 national** (national runs = longer = higher overlap risk; a silent-zero would corrupt the national acceptance gate). Two parts: (a) fail-loud when county resolution yields empty (urgent, contained); (b) session-scope hex_spatial_map or isolate the offending TIGER-repopulating test (bigger). → new task + owner queue.
- **E:101 proof window: 101 done, byte-identical, baseline committed. 101+102 shared window stays OPEN for 102.** → E:102 dispatchable (5433 now free post-archive).

## Wave 12 (2026-07-04 ~03:15) — E:102 DISPATCHED + robustness task #18 created

- **Task #18 created**: harden hex_spatial_map spatial resolution (session-scope + fail-loud) before E:105 — the recurring-contention ROOT. Part (a) fail-loud folded into E:102 as a preamble guard; part (b) session-scoping/offending-test-isolation is #18 (E-lane, gated on E:102 touching the same headless_runner files).
- **E:102 Gamma hydration + scheduled bloc shocks — DISPATCHED** (Sonnet a9fc4d0c, FRESH ~1-2 sprint spec, greenfield speckit). Branch `102-gamma-shocks` off 101@8210db17 in e101, 5433-exclusive. Scope (project/09 L473-490): STEP-0 fail-loud guard on terminal-aggregate serialization (protects its own re-baseline from the E:101 silent-zero bug), then hydrate basket_visibility.py hardcoded α/γ_import/γ_basket=0.68 from spec-100 exposure + fact_hickel_erdi_annual (III.1), then deterministic scheduled bloc shocks (tick,bloc,φ/trade multiplier; no RNG; non-agentic). SHARES 101's proof window — ONE canonical re-baseline covers 101+102 (R-PROOF/ADR053). Briefed with STRICT canonical protocol: determine gamma wiring (baseline-neutral vs re-baseline-with-proof), run canonical 5433-exclusively, MANUALLY verify baseline gated fields NON-ZERO before commit (catch the silent-zero), re-serialize from live session if zeroed, archive after. Gate: shock scenario bends Φ trajectory in Observatory.
- **Active streams: W:093 impl acfb38c9 (5432, ~2-sprint) + E:102 impl a9fc4d0c (5433, ~1-2 sprint). Both fresh long-running specs, different DBs = no contention.** This is max parallelism now — O done; D independent done (068 E-gated); W:094/095/103 serialized after 093; E:104/105 + #18 gated on 102.

## Wave 13 (2026-07-04 ~03:35) — W:093 DONE → REVIEW dispatched

- **W:093 (acfb38c9) DONE.** 10 commits 8044d7c5..24fc95f3, branch `093-territory-org-detail` (clean linear history, no conflict artifacts, no orphan proc). Built: TerritoryDetail+OrgDetail views, map lens set (stance/heat/habitability/faction/collapse over spec-070), get_economy+useEconomy, DE-FIXTURED 5 verb-target endpoints via real GraphProtocol reads (hydrate_state pattern), ColonialStance lowercase-normalize, BreakdownTooltip provenance. Gates PASS: Vitest 417/417 (+37), backend tests/unit/web 268/268, Playwright lens-cycling 2/2, get_economy contract 2/2, `rg 26163 engine_bridge.py` CLEAN, VIII.9 assertion. web:check exit 0 (88 pre-existing warnings).
- **ANOMALY (handled)**: a pre-session orphan agent was independently building the SAME task in w090; work merged cleanly into linear history; W:093 agent adversarially reviewed it, caught 3 defects incl. a CRITICAL wiring bug (DeckGLMap read balkanization from GameSnapshot but it's on GET /map/ metadata — invisible to all tests because they use fake data BYPASSING the real API boundary; fixed b0588ec1). Orchestrator verified: clean tree, linear history, no conflict markers, no orphan still running.
- **REVIEW dispatched** wf_4e36c6e5 (Sonnet, read-only, 5 dims led by api-boundary-integrity — probing for OTHER field-shape mismatches the fake-data tests can't catch, the systemic risk the critical bug exposed; + de-fixture-correctness, map-lens/VIII.9/provenance, data-availability-honesty, test-integrity/merged-coherence).
- **W:093 owner-queue caveats**: (1) ⚠️ NO scenario seeds spec-070 balkanization graph data — seed_influences.json + its pipeline never built → a live seeded game shows "no data" on political map lenses (feature works over API, DATA absent); this is a DATA-sourcing gap affecting "game works locally" — Percy: is seeding balkanization into the product game in-scope for the milestone? (2) MapLegend/HexTooltip per-lens content not built (inline label used); (3) vanguard trend indicators current-value-only.

## Wave 14 (2026-07-04 ~03:55) — E:102 DONE → REVIEW dispatched

- **E:102 (a9fc4d0c) DONE.** 10 commits 8210db17..70e6512a, branch `102-gamma-shocks` (e101, stacked on 101; NOT pushed — orchestrator decides). Built: STEP-0 fail-loud guard `_assert_county_resolution_or_raise` (wired into both terminal-query paths, raises TerminalAggregateResolutionError on hex-rows-exist-but-0-counties; 6 tests incl. non-false-positive) — the robustness part-(b) fix protecting future canonical runs; gamma hydration (SQLite adapter for per-year α + trade-weighted γ_import from spec-100 exposure + fact_hickel_erdi; basket_visibility 0.68-seam killed; is_mvp_fallback=False when hydrated); deterministic scheduled bloc shocks (per-tick external_nodes_phi multiplier; china ×2.5 @tick3, zero leakage). Gates: mise check **8995/0**, qa:e2e-regression **total_v Δ=0.000%**, 16 shock + 6 guard + 34 basket + 7 gamma unit + 5 Postgres integration green.
- **BASELINE-NEUTRAL (verified)**: get_gamma_basket's sole caller TickDynamicsSystem is UNWIRED in the headless runner (ServiceContainer.create never wires melt/basket_calculator) → gamma inert in canonical path → NO re-baseline (like D:098's n_calculator). 101's michigan-e2e.json (a8202ed0, 83/83, 3.126e9) untouched + valid. This CONTRADICTS the master-plan gate's assumption that 102 changes the baseline — but it's correct-by-verification (feature shipped-but-inert until runner wires melt_calculator; owner-queue).
- **DETERMINISM-HASH FINDING (converges with O:099 → systemic)**: agent empirically found tick_commit.determinism_hash AND conservation_audit_log.determinism_hash BOTH embed session_id → ALWAYS diverge cross-run (proven by running unmodified 101 baseline twice) → the III.7 GATE-1 "determinism hash" CANNOT verify cross-run reproducibility. Dovetails with O:099's finding (same hash = contentless sha256(session:tick:seed) identity digest). Agent course-corrected its shock-determinism test to compare persisted VALUES (v_hex_state_asof + DRAIN_EDGE) instead. → OWNER QUEUE (systemic gate weakness).
- **REVIEW dispatched** wf_668511f4 (Sonnet, read-only + SQLite-authorized, 5 dims: gamma-hydration-correctness, shock-determinism-correctness, step0-guard-correctness, baseline-neutrality-wiring-honesty, scope-frozen-determinism-hash). Note: inline workflow first failed on stray inner backticks in template literals → rewrote to scratchpad file + scriptPath.
- **E:102 owner-queue**: (1) gamma hydration unreached by headless runner (wiring = future decision); (2) determinism-hash tables embed session_id (not cross-run comparable — same class as #18's session-scoping); (3) trade_multiplier deferred until 098-LODES activates vol2_step TRADE_EDGE.
- **Active streams: W:093 review wf_4e36c6e5 + E:102 review wf_668511f4 (both read-only, no implementers running — lanes in review state).**

## Wave 15 (2026-07-04 ~04:30) — TOKEN LIMIT → HANDOFF WRITTEN

Owner hit the weekly Claude token limit (resets Jul 7 4am ET). The E:102 fix agent died mid-read (ZERO commits — branch still @ 70e6512a, tree clean); the W:093 fix pass was never dispatched. Both reviews COMPLETED first and both fix briefs are ready+authoritative: `reports/102-fixes.md` (4 confirmed: alpha double-count CRITICAL, shock_schedule missing from manifest input_hash, guard exit-code unreachable, partial-resolution gap) and `reports/093-fixes.md` (12 confirmed incl. 6 critical: de-fixture queries read non-existent node types/wrong attrs/wrong enums → verbs empty in real games; no /map/ contract test; from_graph() cross-lane crash flag). All 6 worktrees verified CLEAN at handoff. **Full resume instructions: `project/_HANDOFF.md`** (status board, environment gotchas, owner queue ×11, exact resume checklist). Session totals: 9 specs fully closed (E:071, W:090/091/092, O:096/099, D:100/098, E:101), 2 built-awaiting-fix (E:102, W:093), O lane 100% done, D independent work 100% done.

## Wave 16 (2026-07-04 ~15:00) — E:102 + W:093 FIX PASSES dispatched in parallel

New orchestrator session resumed from `project/_HANDOFF.md`. Both fix passes dispatched simultaneously (different worktrees, different DBs — zero contention).

- **E:102 FIX PASS DONE** (e101, branch `102-gamma-shocks`). 6 commits `70e6512a..ee357172`:

  - **#1 CRITICAL alpha double-count**: `_DISJOINT_BLOC_IDS = frozenset({1, 7, 9, 10, 12})` — 5 non-overlapping bloc IDs, excluding Europe (id=8, ⊇EU) and ATP (id=5, commodity category). SQLite-verified: Europe $455K ⊇ EU $381K in 2012 data. Re-review caught the initial 6-bloc fix still double-counted EU/Europe → corrected to 5-bloc set.
  - **#2 IMPORTANT manifest hash**: `shock_schedule` added to `deterministic_inputs` (canonicalized/sorted). Test: different schedules → different hash.
  - **#3 IMPORTANT exit code**: `except TerminalAggregateResolutionError` clause added to `main_from_argv`, maps to exit_code=5.
  - **#4 PARTIAL resolution guard**: `null_entity_count` parameter added to `_assert_county_resolution_or_raise`; both call sites count NULL entity rows; raises on partial gaps. 2 new tests.
  - **Minors**: tick=0 rejected (`Field(ge=1)`); Hickel window corrected to 1980-2017 in spec/plan/research; india/latin_america warning on shock construction.
  - **Gates**: 373 targeted tests GREEN, 8 gamma tests GREEN, **qa:e2e-regression Δ=0.000%** (verified twice), pre-commit hooks (ruff/mypy) GREEN on every commit.
  - **RE-REVIEW**: FIX PASS CLEAN after alpha correction. All 5 dimensions CONFIRMED.

- **W:093 FIX PASS DONE** (w090, branch `093-territory-org-detail`). 4 commits `24fc95f3..e3024c8c`:

  - **#1-4 CRITICAL de-fixture queries**: All 4 corrected against engine's real graph-writing code:
    - `get_educate_targets` → queries `social_class` nodes (correct proxy since bridge doesn't access XGI hypergraph)
    - Extractive-edge filter → reads `edge_mode` attribute (EdgeMode, not EdgeType) with `edge_type` fallback
    - `extraction_intensity` → reads from Territory nodes (where ProductionSystem writes it)
    - `get_mobilize_targets` → uses `"civil_society"` (correct OrgType enum value)
  - **#5 CRITICAL API boundary**: MSW handler for `/api/games/:id/map/` + contract test exercising real fetch pipeline.
  - **#7-9 Map lenses**: Faction-picker UI wired (select in MapModeSelector when faction lens active); Collapse-Moment lens distinct (reads `contested`); Economy BreakdownTooltip honestly deferred (no ScriptValue selector for /economy/ endpoint).
  - **#10-11 Data honesty**: `isBalkanizationEmpty()` detects structurally-empty block (all 3 arrays empty); Playwright + unit test inject empty-but-present shape.
  - **Dead code removed**: `_communities_in_territory` + `_avg_agitation_for_members` (zero call sites after de-fixture).
  - **Gates**: 272 backend tests GREEN, 417 Vitest GREEN (3 pre-existing failures in spec-092 `tick-resolution-page.test.tsx` — not touched by 093), `rg '26163'` CLEAN.
  - **RE-REVIEW**: FIX PASS CLEAN. All 6 dimensions CONFIRMED. 4 minor cleanup items (all addressed or documented).

- **CROSS-LANE #6 ASSESSMENT**: `WorldState.from_graph()` crash on faction/sovereign/community nodes — **LATENT, NOT BLOCKING**. Verified: no production code path adds faction/sovereign/community nodes to the main graph (`WorldState.to_graph()` creates only social_class/territory/organization/key_figure/institution/industry; `load_seed_factions`/`load_seed_sovereigns` defined but never called from graph-seeding code). Valid tech-debt for when spec-070 graph integration lands. → OWNER QUEUE (latent).

- **E:102 + W:093 COMPLETE** (tasks #15 and #7). Both ready for BD merge.

## Wave 17 (2026-07-04 ~20:00) — E:068 completion + W:094 The Wire

Both specs dispatched in parallel (E-lane 5433, W-lane 5432 — zero contention).

- **E:068 BEA I-O COMPLETION SLICE — DONE** (e101, branch `068-bea-io-completion-slice`). 5 commits `ee357172..3dad3896`:

  - **T056**: `BEAShareLookupService` wired into `hydrate_hex_state()` via new `bea_share_service` keyword param. Per-county `lookup_county_share(fips, year).intermediate_inputs_share` replaces the hardcoded `_INTERMEDIATE_INPUTS_FRACTION = 0.5`. `None` default preserves backward compat (0.5 fallback per FR-010).
  - **T057**: Production call site `postgres_initialization.py:713-731` constructs `DefaultBEAShareLookupService` from SQLAlchemy Session on the reference DB.
  - **T058**: SC-005/SC-008 audit gates wired. `stale_share_summary.py` computes `affected_employment_fraction`; fixed Python falsy-zero bug.
  - **BASELINE CHANGE (R-PROOF)**: stddev(c/v) across 83 MI counties: 0.271 → **0.393** (SC-005 ≥ 0.2 PASS). `total_c` +8.64% (Michigan 520t) / +10.66% (tri-county 5t). Proof documented in `specs/068-bea-national-io-ingest/proof.md`.
  - **068 SESSION ARCHIVED** (Wave 18 remediation): session `ee05760c` archived to Parquet, 9 partitions purged from 5433.

- **W:094 THE WIRE — DONE** (w090, branch `094-the-wire`). 8 commits `e3024c8c..8a521a33`:

  - **NarratorProvider interface**: Python Protocol + `DeterministicNarrator` (template-based, no LLM, no RNG, zero `babylon.*` imports). Determinism test asserts byte-identity. Provider-swap: `EngineBridge.__init__` accepts `narrator` param.
  - **4-tab UI**: WIRE triptych (Continental/Liberated/Intel), INDEX, PATTERNS, CORPUS. Cold Collapse tokens. Euphemism sync-highlight. MSW handlers + contract tests.
  - **Gates**: Backend 294/294, Vitest 427/430 (3 pre-existing), narrator-determinism PASS, provider-swap PASS, Article III PASS.

## Wave 18 (2026-07-05 ~12:00) — W:095 + E:104 + E:105 + REMEDIATION

### W:095 Endgame Chronicle + Dialectic — DONE (w090, branch `095-endgame-chronicle`)

- 4 commits `8b7252e5..13438ac5`. Speckit + RED tests + GREEN implementation.
- 3 bridge methods: `get_endgame_state`, `get_journal_objectives`, `get_contradiction_snapshot`.
- 3 frontend screens: Dialectic (contradiction layer), Chronicle (end-state), Objectives (Vic3-style).
- EndgameDetector 3-of-5 bridge bug fixed (all 5 terminal outcomes now surface).
- EndgameDetector priority discrepancy: **FLAGGED cross-lane** (engine code docstring is stale, code follows FR-033 order).
- **Review: WITH CONCERNS** (non-blocking). Backend 311/311, Vitest 433 (3 pre-existing reds).

### E:104 National Tick-Compute + #18 — PARTIAL (e101, branch `104-national-tick-compute`)

- 2 commits `3dad3896..cce596db`.
- **#18 hex_spatial_map**: Found and isolated the `fresh_tiger_table` fixture (used `TRUNCATE` with `autocommit=True`). Fixed via `PinnedPool` wrapper (transaction-scoped, `DELETE` not `TRUNCATE`, `ROLLBACK` on teardown). **BUT**: this only isolates ONE fixture; hex_spatial_map remains a global table — the structural hazard persists for E:105.
- **E:104 national profile**: **DID NOT RUN** — national hex hydration timed out at 10+ minutes (3,156 counties, ~3.17M hex cells). The budget is a 5-tick Michigan-statewide proxy ×2, NOT a national measurement. The `qa:tick-budget` checker has no tests and isn't in CI.
- **Per-system timing**: Already wired by spec-065 T074 (not E:104's work). Verified working.
- **DecompositionSystem no-op**: Already closed by spec-071. No work needed.
- **Review: WITH CONCERNS** — budget completeness (3/26 systems missing), measurement variance (2× between runs), default scope mismatch.

### E:105 National Canonical Acceptance — PARTIAL (e101, branch `105-national-canonical-acceptance`)

- 7 commits `cce596db..64bc0d9f`.
- **National run DID NOT COMPLETE** — tick loop too slow (>30min/tick at national scale). ContradictionFieldSystem scales super-linearly (O(N²) or worse). Projected 200-tick runtime: ~100 hours.
- **What was delivered**: `--liveness-gate` flag (scope-agnostic runtime assertion), national-scale bug fixes (4-digit IDs, bourgeoisie offset), storage budget refresh.
- **What's missing**: The actual national canonical run, the national baseline bundle, Observatory verification of a national session.
- **BLOCKERS**: ContradictionFieldSystem optimization (owner-queue #16) + national hex hydration optimization (owner-queue #17) needed before the national run can complete.

### W:103 Trade Surfaces — DONE (w090, branch `103-trade-surfaces`)

- 5 commits `13438ac5..290218d9`. Speckit + backend + frontend.
- 3 trade surfaces: Wire INDEX (per-bloc flow lines), Territory Detail (import-exposure provenance), Analysis (trade panel).
- 3 bridge methods + 3 Django views + 3 MSW handlers. Backend 329/329, Vitest 437 (3 pre-existing reds).

### P0 REMEDIATION (2026-07-06)

**Four P0 findings from independent audit:**

1. **P0-1 FIXED**: E:102 alpha still double-counted — Pacific Rim (id=10) + Asia (id=12) overlap. The 5-bloc sum ($2.78T) exceeded total real US imports ($2.75T). Fixed: dropped Pacific Rim → 4-bloc set {1, 7, 9, 12}. SQLite-verified: 4-bloc sum = $2.02T (undercount, not double-count). Only harmless today because gamma is shipped-but-inert (Percy ruled "wire now" — item #9).

1. **P0-2 FIXED**: E:105 red unit test — `test_run_simulation_signature_is_preserved` failed because `scope_name` param was added to `run_simulation` but the canonical signature pin wasn't updated. Fixed: updated pin to include `scope_name: 'str' = 'detroit-tri-county'`. Also: hot 068 session archived to Parquet, 0-row national partition corpse dropped from 5433.

1. **P0-3 ASSESSED**: #18 only isolated one test fixture; hex_spatial_map is still a global table. The structural hazard (any process can wipe it, affecting all concurrent sessions) remains. Session-scoping would require a schema change (owner-queue item A). The STEP-0 guard from E:102 catches the symptom (silent-zero terminal aggregates); #18 prevents one known cause. Full session-scoping is an open engineering task.

1. **P0-4 DOCUMENTED**: W:095 had zero governance trail (progress.md not updated, spec docs incomplete). Code itself is honest (engine untouched, bridge reports engine's own verdict, §6.6 endgame decision not usurped). Gates pass when run (backend 311/311, Vitest 433 green with 3 pre-existing reds). Governance trail backfilled in this wave.

### Owner Queue Made Durable

`project/owner-queue.md` created with all 21 items (11 original with Percy's rulings + 10 new from Waves 16-18) and 6 new decisions needed from Percy.

## Wave 17 (2026-07-04 ~20:00) — E:068 completion + W:094 The Wire

Both specs dispatched in parallel (E-lane 5433, W-lane 5432 — zero contention).

- **E:068 BEA I-O COMPLETION SLICE — DONE** (e101, branch `068-bea-io-completion-slice`). 5 commits `ee357172..3dad3896`:

  - **T056**: `BEAShareLookupService` wired into `hydrate_hex_state()` via new `bea_share_service` keyword param. Per-county `lookup_county_share(fips, year).intermediate_inputs_share` replaces the hardcoded `_INTERMEDIATE_INPUTS_FRACTION = 0.5`. `None` default preserves backward compat (0.5 fallback per FR-010).
  - **T057**: Production call site `postgres_initialization.py:713-731` constructs `DefaultBEAShareLookupService` from SQLAlchemy Session on the reference DB. `test_tiger_ingestion.py` left on `None` default (geometry test, not BEA).
  - **T058**: SC-005/SC-008 audit gates wired. `stale_share_summary.py` computes `affected_employment_fraction`; fixed Python falsy-zero bug (`float(0.0 or 1.0)` → `1.0`, now explicit `None` check).
  - **BASELINE CHANGE (R-PROOF)**: stddev(c/v) across 83 MI counties: 0.271 → **0.393** (SC-005 ≥ 0.2 PASS). `total_c` +8.64% (Michigan 520t) / +10.66% (tri-county 5t). `total_v`/`total_s`/`total_k`/`max_tension` unchanged. Liveness 83/83 throughout 520 ticks. SC-008: 0% affected employment (PASS). Proof documented in `specs/068-bea-national-io-ingest/proof.md`.
  - **Tri-county catch-up**: `detroit-tri-county-5t.json` `external_node_flows` also updated from stale county-FIPS entries to spec-101/102's bloc-level entries (one-time catch-up, documented in proof.md).
  - **RE-REVIEW**: WITH CONCERNS → both addressed: (1) proof.md written; (2) tri-county drift documented. Core wiring CLEAN (all 5 dimensions confirmed). 46 tests pass (12 skipped — TIGER shapefile absent, pre-existing).
  - **E:068 COMPLETE** (task #13). Ready for BD merge.

- **W:094 THE WIRE — DONE** (w090, branch `094-the-wire`). 8 commits `e3024c8c..8a521a33`:

  - **Speckit created**: `specs/094-the-wire/` with spec.md (11 FRs, 7 SCs), plan.md, tasks.md (29 tasks), research.md (9-file mockup analysis), contracts/wire.yaml (OpenAPI 3.1).
  - **NarratorProvider interface**: Python Protocol at `web/game/narrator.py:1070` with `narrate(events, meta, visibility=None) -> dict`. `DeterministicNarrator` implementation: template-based, no LLM, no RNG, zero `babylon.*` imports (Article III structural test PASS).
  - **Determinism**: Same events → byte-identical output (json sort_keys comparison). Provider-swap: `EngineBridge.__init__` accepts `narrator: NarratorProvider | None` param (defaults to `DeterministicNarrator()`); test injects `MockNarrator` through bridge and verifies `get_wire_feed` uses it (review fix).
  - **Wire feed endpoint**: Django view `game_wire` at `/api/games/:id/wire/`; bridge `get_wire_feed` consumes real `tick_event` data via `get_journal_dashboard`; data shape matches mockup's `WIRE_DATA` structure (meta, index, euphemisms, story, filters). MSW handler + contract test.
  - **4-tab UI**: WirePage with WIRE triptych (Continental/Liberated/Intel columns), INDEX (story archive), PATTERNS (Manufacturing Consent dashboard with 5 filters), CORPUS (RAG browser). Cold Collapse tokens throughout. Euphemism sync-highlight (hover in one column drives highlight in others). NavRail entry added.
  - **Gates**: Backend 294/294, Vitest 427/430 (3 pre-existing tick-resolution-page failures), narrator-determinism PASS, provider-swap PASS, Article III PASS. Playwright owner-run (gated on SPEC061_TEST_SESSION_ID).
  - **RE-REVIEW**: WITH CONCERNS → provider-swap fixed (bridge now accepts injected narrator). Core dimensions CLEAN (determinism, endpoint, UI fidelity, constitutional compliance all confirmed). 20/20 narrator tests pass.
  - **W:094 COMPLETE** (task #8). Ready for BD merge.

- **Session totals**: 13 of 13 program specs that were in-flight or pending are now COMPLETE. All 4 lanes have their current specs done:

  - E lane: 071 ✅ → 101 ✅ → 102 ✅ → 068 ✅ → [104 → #18 → 105]
  - W lane: 090 ✅ → 091 ✅ → 092 ✅ → 093 ✅ → 094 ✅ → [095 → 103]
  - D lane: 100 ✅ → 098 ✅ → 068 ✅ (E-gated, now done)
  - O lane: 096 ✅ → 099 ✅ (100% done)
