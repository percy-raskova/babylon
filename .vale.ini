# Merge with the global Vale configuration. These two exact files have literal
# machine identifiers in YAML frontmatter. Disable only the rule that mistakes
# each identifier for prose; all other global rules remain active.

[.opencode/skills/specification-discover/SKILL.md]
ste.UnapprovedWords = NO

[.opencode/skills/specification-validate/SKILL.md]
write-good.TooWordy = NO
