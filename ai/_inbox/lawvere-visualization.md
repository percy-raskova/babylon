I pulled the history. What's already settled: deck.gl H3HexagonLayer on a dark MapLibre basemap, Sigma.js/Graphology for topology, Recharts for time series, the red/grey/yellow/black Constructivist palette with the Tufte rules (color encodes class position, luminosity encodes magnitude, no glow that isn't data). And critically, the field-theory spec you wrote in February explicitly deferred "GUI visualization of fields or gradients" to dashboard features. So this question is that deferred spec. My recommendation is to not design visualizations one at a time — design a **visual grammar** where the rendering rules *are* the physics, then build instruments inside it.

Four grammar laws first, because everything else hangs off them.

**Extensive renders as stuff, intensive renders as color.** Labor-hours, value masses, population, Φ flows: particles, flux width, stacked volume on hexes. Rates — r, s/v, OCC, κ, heat, contradiction intensity — hex fill and edge hue only. The payoff is that your conservation invariant becomes *perceptible*: when the player zooms H3 res 7→6→5, hexes merge and the stuff visibly sums (total ink conserved), while the color fields re-render rather than stack. Players learn the physics by using the camera: flows conserve, rates don't. This is the Lawvere variance discipline enforced at the presentation layer — you literally cannot missum an intensive because color doesn't add.

**Quantities flow, qualities cut.** Continuous animation is reserved for field evolution and value flows. Every enum flip — edge mode transition, consciousness tendency, phase state, coefficient reset — renders as a hard cut, one frame to the next, no tweening. Crisis is a full-screen rebase: palette re-normalizes, layout re-settles. This puts Anti-Pattern VIII.1 into the visual grammar itself; a player who never reads the docs still absorbs "qualitative change is discontinuous" because the screen never blends between qualities.

**One motion budget per tick.** Your field spec already defines the principal contradiction as max |df/dt|. That's the only thing that pulses. Everything else is static between ticks. Tufte-compliant, and it makes the principal contradiction do actual player-facing work.

**Curvature is terrain, not decoration.** κ is recomputed only on topology change — which means the geometry deforms only when someone *acts*. Use that.

Now the instruments. **Terrain**: render solidarity-subgraph edges with κ driving width and tautness — negative-curvature bridges thin, taut, yellow-hot; positive-curvature cluster edges thick and slack. Eventually use a curvature-informed layout so clusters sit as basins and bridges as passes; organizing visibly reshapes the manifold, repression visibly cuts the taut edges. **Weather on the terrain**: the five contradiction fields as toggleable hex layers, with your continuity equation rendered literally — contradiction particles flowing down ∇f each tick, and negative-curvature edges shown damming the flow: a sharp color cliff across the Wayne/Oakland bridge where the gradient can't equalize. That's your February synthesis (curvature traps gradients) made visible without a word of explanation.

**The organization gauge** — this is the cylinder, player-facing. For any selected community hyperedge, render the XGI hull as a translucent region (material basis, the ∇ scope), actual solidarity edges inside it, and the missing intra-hull edges as faint ghost edges — the cokernel of the unit map, the terrain of struggle. Annotate |Π₀| of the induced subgraph. The inspector shows a small triptych: atomized dots | actual graph | filled hull. Never say "adjoint cylinder" to the player; say atomization ↔ organization. Class-in-itself is a hull with scattered pieces; for-itself is one piece. Ghost edges becoming real is what Educate and Mobilize *look like*.

**The bifurcation instrument.** The George Jackson question reduces to Π₀ of the cross-colonial-divide solidarity subgraph, so give it the most privileged fixed position in the HUD: two masses (Macomb white proletariat, Wayne New Afrikan communities) with the count and total curvature of edges between them, small-multiplied over recent ticks. Zero cross edges approaching crisis = the fascism route, visible at a glance. This is cheap to build and it's the single most important number in the game.

**g₃₃ as opacity.** Department III flows render at opacity proportional to g₃₃. Reproductive labor is enormous and nearly invisible at start — faintly present to the player (the theorist knows it's there), absent from the price-accounting panels until visibility rises and the flows solidify into priced streams that compress profits. The price system's blindness, made literal.

**The state's view.** Your repression doc already specced the Bondi aesthetic — build it as a toggle. Same graph, edge semantics flattened to grey "association," centrality highlighted, heat overlay, targeting annotations. The player's view sees typed modes; the state's sees topology. Two lenses on one dataset, and it teaches OPSEC for free: your high-betweenness organizer glows in the enemy's lens.

Sequencing against your phased plan: the grammar laws plus curvature-colored edges and the bifurcation instrument are cheap — the engine already computes κ, Π₀ (your β₀), fields, gradients; this is JSON plus render rules. The hull/ghost-edge gauge is moderate. Ricci-flow terrain layout and the COP toggle are post-MVP. All of it respects AI-observes-never-controls: pure derivation from state.

Want this written up as the deferred visualization spec?
