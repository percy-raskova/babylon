# The Standing Loop Goal — the Playable-Game Program

Set by the Director 2026-07-29. This file is the canonical goal text: re-arm any session with
it verbatim. The `loop-contract-playable-game` memory instructs future sessions to honor it.

---

Work the Babylon playable-game program until the vision is COMPLETE. Binding law:
docs/superpowers/specs/2026-07-29-game-design-standard-design.md (the Game Design
Standard) + CONSTITUTION.md; running evidence: reports/loop-digest.md.

TERMINAL CONDITION — satisfied ONLY when ALL of the following hold, each proven by
committed, replayable evidence, none by assertion:
(a) v1.0 per Amendment AE: the Rust engine RELEASED — P27 Phases 1–4 complete with
    every playable-slice exit gate green (Standard §10), the cutover ceremony signed,
    the Python engine retired;
(b) a stranger can install Babylon on a clean Linux/macOS machine (player-tier
    Postgres cluster lifecycle, disk preflight) and play one continuous seeded
    campaign to its conclusion — the 100-year horizon or the Council rite;
(c) the agent-playthrough estate holds archived (seed, action-log, verdict) triples:
    full 5,200-tick campaigns COMPLETE; each of the five patterns WITNESSED in ≥1
    archived run; cadence gates measured green (≤150 autopauses/century, no 20-tick
    zero-decision window, no dead 15-minute mid-game stretch); honesty gates green
    (every interception legible, every ineligible verb shows its remedy);
(d) verification layers green: null-hypothesis Marxist invariants + federal-data
    tracking; contrapositive rupture genealogies; the III.13 vault byte-gate;
(e) all four loops LIVE: the doctrine surface (4 trunks, 12 (Major,Minor) identities
    passing the divergent-fingerprint gates), the ruled verb estate including the
    strike lane, the multi-res camera (reduction test + hash riders landed), the
    Layout-B instruments (Verdict Watch / Chronicle / Intercepts), the narrator
    required-with-fallback (NarrationEnvelope + goldens + the epilogue Book), and
    Wave-3 game-feel (all 39 SFX + 13 tracks wired);
(f) the Wiring Completeness ledger holds ZERO un-dispositioned rows; the
    reachability + fallback-coverage sentinels green repo-wide;
(g) every director-gate issue (#376–#383 and all successors) is RULED BY THE DIRECTOR
    and its consequences implemented — never self-decided, never expired;
(h) the Director has played an eyes-on acceptance session and recorded approval via
    the release ceremony (docs/versioning.md).

EACH ITERATION: (1) sync project 8; pick the highest-leverage UNBLOCKED item per the
Standard's sequencing (sentinels → density/NarrationEnvelope → verb 3×3 + G3 →
spatial/Phase 0-D → P27 phase gates); (2) execute it TDD, mise run check green, commit
per unit, self-merge impl PRs on green per standing ruling; (3) update project 8 +
ai/state.yaml with evidence; (4) queue Director-reserved matters as director-gate
issues and keep working other lanes — never decide them, never stall on one;
(5) red gate or contradiction → stop THAT item, file it, move on; (6) append a
digest to reports/loop-digest.md. NEVER weaken, skip, or reinterpret a gate to
approach this goal — gates only tighten, and amendments belong to the Director.
Heavy test/build runs stay single-flight; never fan out pytest. If EVERY lane is
blocked on Director rulings, compile the pending-rulings brief, notify, and wait
for input.
