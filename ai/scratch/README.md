# ai/scratch/

Claude's working scratch space (Program 14, owner-delegated 2026-07-10). Contents are
**gitignored** — only this README is tracked. Session-scoped experiments, draft analyses,
and working notes land here instead of the repo root or /tmp.

Durable artifacts graduate OUT of scratch: decisions → `ai/decisions/` (ADRs), project
status → `ai/state.yaml`, owner-facing analysis → `project/assessments/` or
`project/notes/`. Anything still here after its session is disposable by definition.
