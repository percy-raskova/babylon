# ~/.config/babylon/credentials — secrets ONLY, read only by the intelligence
# lane (§A3: the engine never sees this file; II.5 in filesystem form).
#
#   install -m 600 credentials.example ~/.config/babylon/credentials
#
# Never commit this file. Never put it in the Nix store. Never mix it with
# config.toml or defines.yaml.

[cloudflare]
# Babylon beta key for the babylon-api Worker (issued by seed-beta-key.sh).
api_key = "bk_replace_me"
