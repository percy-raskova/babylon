# Loop Digest — the Playable-Game Program

Standing loop (Director, 2026-07-29): work the playable-game program under the Game Design
Standard + Constitution; director-gate issues for reserved lines; digest per iteration here.

---

## 2026-07-29 — Iteration 0: the Standard exists

- **Interview + design session complete.** Sixteen Director rulings captured live (sandbox/horizon-verdict endings, four co-equal loops, one-org-any-line, (Major,Minor) doctrine over 4 trunks, multi-res native, start-local campaign, 30–80 h grand campaign, narrator-with-fallback, negative Φ real, design-standard-first sequencing). Interface Layout B chosen in the visual companion.
- **Three research workflows, 39 agents, 0 errors, ~4.4M subagent tokens:** the estate study (11 readers), the design-inputs deep dive (multi-res judge panel, doctrine pairs, ending reachability, density, verbs, narrator), the Postgres deep dive, plus the wiring-completeness study.
- **The Game Design Standard committed:** `docs/superpowers/specs/2026-07-29-game-design-standard-design.md` — §0 rulings ledger, §§1–10 design, §11 Director Decision Queue (33 items). §§1–7, 10 Director-approved live; **§8 (Persistence) and §9 (Wiring Completeness) await Director review** (§11 item 33).
- **Evidence preserved:** `reports/design-inputs-dossier-2026-07-29.md`, `reports/wiring-completeness-2026-07-29.md`, `reports/postgres-brief-2026-07-29.md`.
- **Load-bearing discoveries:** four of five endings provably unreachable (one unexecuted plan + six missing writers); ~110 declared-but-dead constructs across 25 ledger rows; a live torn-tick save-corruption path (topology written outside the commit envelope); 91% of the runtime DB is duplicated reference data; every org congresses the same week (single global scalar); the strike wire's rent-pool cascade routes agitation to revolution *because the bribe stops*.
- **Next iterations:** project-8 hygiene (add P27 issues; file the 33 director-gate items), then first moves: the Tier-1 reachability sentinel + fallback-coverage sentinel, then the projection-layer density fixes + NarrationEnvelope schema.

**Director attention requested:** read the spec once (§§8–9 + the Decision Queue); rule the queue at leisure — every item is filed as a director-gate issue and nothing blocks on any single ruling.

## 2026-07-29 — Iterations 1–2: the board is truthful and the first gate exists

- **The Standard is law**: PR #375 MERGED to dev — the Game Design Standard + all three evidence dossiers.
- **Iteration 1 (board sync)**: director-gate issues **#376–#383** filed and added to project 8 (endings/verdicts, doctrine surface, strike & verb algebra, multi-res & spatial data, pacing/density/long-wave, narrator, persistence & CI, spec §8–§9 review); P27 issues #343/#368 added to the board (source-of-truth gap closed).
- **Iteration 2 (first move, TDD)**: the **reachability sentinel** — PR **#384** (auto-merge armed). New `babylon.sentinels.reachability` family: every EndgameDetector gate operand needs a detected runtime writer, a machine-verified cited writer, or a cited ledger disposition; stale ledger rows and unregistered detector reads red the gate. 16 tests incl. real-tree conformance; wired into `check:sentinels` + `mise run check:reachability`. The sentinel corrected its own draft registry twice (sovereignty_type/extraction_policy/ruling_faction_id ARE written; habitability is helper-mediated) — the ledger now holds exactly the three genuinely writer-less operands behind the dead endings.
- **Scar recorded**: worktree commits need `uv sync --extra server --frozen` first and `UV_FROZEN=1 git commit` — bare `uv run` in a worktree re-locks against `../hypergraph-rs` and trips pre-commit's modified-files check.
- **Next**: fallback-coverage sentinel (34/98 EventTypes rendering generic; 19 dropping at the bus boundary), then the projection-layer density fixes + NarrationEnvelope schema.

## 2026-07-29 — Iteration 3: the queue is EMPTY and the Standard is RATIFIED

- **The Director disposed the entire §11 Decision Queue live** — 38 rulings in nine batches, recorded on issues #376–#382 the same hour and constitutionally in **ADR176**. Headlines: the Long Containment is the named sixth ending; George Jackson routes to RED_OGV *named plainly*; AUTONOMIST is the fourth trunk (names kept bold); choose-Major-earn-Minor with transposition-default; MILITANCY cuts both ways; **the bribe, not the fight, sets the core wage**; funding is a doctrine-pair surface (reformist money = institutional capture-vector); BUILD goes live; clandestinity AND the metabolic surface chartered; kinded dispersion fields; Phase 0-D blocks LOD with the mask in scope; RNG/grain/MIN riders authorized; soft-float libm; standing orders + all three restoration channels; earned-epistemic-position narrator (the wire flatters, the underground is blunt); all three persistence defects fix NOW; checkpoint-only hydration is BINDING; PG 17 + CI fork + digest-pin authorized.
- **§8 and §9 APPROVED → the Game Design Standard is RATIFIED IN FULL** (#383 closes); status flipped in the spec, disposition record added to §11.
- **PR #384 MERGED** — the reachability sentinel is live on dev.
- `ai/loop-goal.md` committed — the canonical vision-complete goal text, re-armable by any session.
- **Next**: fallback-coverage sentinel (Iteration 4); then the P-J defect train (torn tick first); then the rulings-consequences trains (Autonomist transcription + tag rider, strike lane's typed motions, RNG rider before Phase 1 Task 5).

## 2026-07-29 — Iteration 4: the player hears what fires, and what cannot fire is ledgered

- **The 16 P25 electoral events render bespoke** — `ELECTION_HELD`, `GOVERNMENT_FORMED`, the three `POLICY_*` outcomes, `CAPITAL_STRIKE`, `LINE_STRUGGLE_SPLIT`, the disillusion/hope/betrayal family — 80/100 wire-reaching EventTypes now show real content instead of the generic field-list line. Their generic rendering was a dated fact (the adapter predates P25's tested builders), not a design choice; the Baker-equivalence static cross-check (re-pinned 64→80) proves every new summary reads only wire keys `EVENT_BUILDERS`' own builders read.
- **The fallback-coverage sentinel is live** (`babylon.sentinels.fallback_coverage`, 19th static sentinel, `check:fallback-coverage`): ADR176 ruling 25's two verdict-surface checks, promoted from the seam sentinel's advisory tier to a GATE with the Phase-0 disposition pass shipped as a 20-row cited ledger — **5 CHARTER** (ENDGAME_REACHED, RED_OGV_ENDGAME, FRAGMENTED_COLLAPSE_ENDGAME, PATTERN_SHIFT → the endings train, rulings 1–3; INFRASTRUCTURE_CHANGE → the BUILD lane, rulings 15/37) + **15 DORMANT** (dead vocabulary, scan-cited, pending W.2 disposition). Ratchet: coverage arriving for a ledgered member reds the gate until the row retires; the inverse check catches crafted-but-unreachable summaries (a bespoke line for an event that can never arrive).
- **Load-bearing truth surfaced**: every endgame event was in the DROPPED class — the verdict surface died at the bus boundary, one level above the writer-less gate operands Iteration 2 ledgered. The endings train now owns both layers via cited CHARTER rows.
- **Superseded advisory retired**: seam's `check_event_coverage` (+ its module-wide AST helper) removed in the same motion — the precise dict-key scanner replaced it.
- **Next**: the P-J defect train (torn-tick envelope fix FIRST — red-phase test reproducing the tear), then the CI authorizations train (PG 17, CI config fork, digest-pin ceremony, un-gate tier under the gates-only-tighten license).

## 2026-07-29 — Iteration 5: the save can no longer lie

- **The torn-tick defect is CLOSED** (ADR176 ruling 28's first freeze exception; the brief's only defect-not-optimization finding). The session used to write the adjudicating topology in its own transaction, separate from the envelope + `tick_commit` marker — a crash between the two left `node_state` rows for a tick whose marker never landed, and `hydrate_graph(tick=None)` resolved "latest" via `MAX(tick) FROM node_state`, resuming the engine from the torn tick. Now: `persist_tick_atomic` carries `graph=`/`events=` inside its ONE transaction (monotonic-idempotent semantics preserved), `advance_tick` + the tick-0 seed collapse to the single call, and **`tick_commit` is the only adjudicator of "latest"** (the `node_state` MAX survives solely for pre-0029 databases where markers cannot exist).
- **TDD**: the red-phase test reproduced the tear against the unpatched tree; `test_torn_tick_recovery.py` now pins the law — torn topology never adjudicates, a mid-envelope failure leaves zero topology rows, forensic explicit-tick reads unchanged. `qa:regression` 11/11 byte-identical + determinism leg; vault byte-gate green.
- **Two pre-existing reds surfaced and dispositioned** (both reproduce on the unpatched tree): **#388 filed** — `persist_tick(events=...)` is broken on every runtime-schema-only DB because the `simulation_event` ON CONFLICT unique index exists only in the legacy web/Django chain (the CI-authorizations train will surface this the moment the PG tier un-gates); and the known spec-088 S3 `hex_spatial_map` contention (headless shock-determinism suite, local env).
- **Iteration 4 landed**: PR #386 MERGED; board issue #387 closed with evidence; #376/#380 moved In Progress.
- **Next**: P-J's remaining two defects — the archival purge-verification skip, then the missing `rng_seed`+ContentDigest on CampaignRecord (both ruling 28); then the CI-authorizations train (PG 17, CI config fork, digest-pin ceremony, un-gate tier).

## 2026-07-29 — Iteration 6: the P-J defect train is COMPLETE

- **Defect 2/3 — the purge gate fails closed** (brief item 15): `_verify_manifest_against_live` treated a manifest table absent from the live DB as `continue` — passing the completeness gate on exactly the rows it could no longer verify. Now `ArchiveVerificationError`, nothing deleted; the red-phase fake-conn test proves the count query never even runs for a missing table.
- **Defect 3/3 — the campaign catalog carries its replay identity** (brief gap 1 / item 16): `rng_seed BIGINT` + `content_digest TEXT` on `babylon_meta.campaign` (fresh DDL + digest-stamped healing ALTERs + migration 0042, all rerunnable); `stamp_replay_identity()` fills NULL in place, idempotent, and REFUSES a rewrite (III.7 — a campaign's replay identity never changes); play-boot stamps every campaign from its own `game_session.rng_seed`, backfilling pre-column rows. "Save lost" becomes "rebuild save." `content_digest` holds the canonical P27 `ContentDigest` serialized form (determinism-contract.rst) and stays honestly NULL in the Python era — `rules_hash` hashes BSL rule content that predates the Rust engine, and a fabricated stand-in would poison the canonical value.
- **Ruling 28 is now fully implemented** (torn tick #389 + these two) — consequence 3/3 posted to #382. Byte-gates green both iterations (qa:regression 11/11 + determinism leg; vault byte-identical).
- **Next**: the CI-authorizations train (ruling 33: PG 17 move, CI config fork, postgres image digest-pin ceremony, un-gate the PG tier — which will surface #388); then ruling 32's retention/preflight features (1-live-session enforced in code, disk preflight + mid-run warning); then the density/NarrationEnvelope lane per Standard sequencing.

## 2026-07-30 — Iteration 7: the database moves to the shipping major and CI finally watches it

- **#388 CLOSED** — `simulation_event`'s natural-key unique index (the ON CONFLICT idempotency contract's target) now ships with the runtime schema as a dedupe-then-index chunk + migration 0043; it existed only in the legacy web/Django chain, so `persist_tick(events=...)` raised `InvalidColumnReference` on every runtime-schema-only database. The three formerly-red tests turned green with **no code change**; 4 structural pins added.
- **The CI-authorizations train landed** (ruling 33, all four legs): **PG 17 move** (postgis/postgis 17-3.5; the player ships 17 via the flake — dev/CI on 16 left every plan shape unverified against the shipping major); **image digest-pin as a ceremony** (`FROM tag@sha256`, bump procedure documented in the Dockerfile); **CI config fork** (`postgresql.ci.conf` runner-sized + `docker-compose.ci.yml` tmpfs datadir, loud-crash `restart: no`); **PG-tier un-gate** (`ci.yml` `pg-integration` job running the DECLARED `test:integration-pg` subset — persistence + tick_commit + monotonic + archival, data-drive-free — against the same pinned image + initdb the dev box uses).
- **Validated live on PostgreSQL 17.10**: declared subset 85/85; `qa:regression` 11/11 byte-identical + two-process determinism leg; vault byte-gate byte-identical. The byte-identity of the goldens across the major bump is itself the evidence the brief demanded.
- **Local migration was ADDITIVE**: PG16 datadir preserved untouched at `/media/user/data/babylon-pg` (Director deletes at leisure — ~6 GB, 106 disposable dev sessions); PG17 initdb'd fresh at `babylon-pg-17`. One misfire self-caught: a `db:up` from the main checkout initdb'd a PG16 scratch cluster onto the new dir first — verified minutes-old via read-only container, wiped, redone from the branch.
- **Follow-up queued**: flip `pg-integration` to a REQUIRED check after its first green run on this PR (tightening, not before it exists — a required check that never ran blocks every merge).

### Iteration 7 addendum — the maiden run's three-layer convergence, then green

- **PR #391 MERGED** (00:18): the maiden `pg-integration` run needed three repair cycles, each finding a real gap — (1) the repo-hygiene root allowlist (one cause, two red checks); (2) a genuine **compose v5.3.1 defect**: `config` renders a tmpfs-over-bind merge correctly while `up` silently keeps the base mount even under `--force-recreate` — replaced with the base file's own convention, `BABYLON_PG_DATA=babylon-pg-ci` as a named volume (fresh by construction on ephemeral runners), proven 85/85 on the exact CI shape locally before pushing; (3) the integration conftest imports Django GIS → the job needed `gdal: "true"` like test-unit. **Ruling 33 is fully implemented.**
- **Governance finding, Director-optional**: `dev` has NO branch protection — auto-merge waits on checks by repo behavior only. Making checks hard-required is the Director's call (posted to #382 with a suggested list); not self-decided.
- **Next**: ruling 32 features (1-live-session retention ENFORCED IN CODE + disk preflight + mid-run warning); then the density/NarrationEnvelope lane (next Standard phase).

## 2026-07-30 — Iteration 8: the database earns its "100% in the background"

- **Ruling 32 is fully implemented**, in four units on PR (auto-merge): (1) **archival coverage became schema-driven** — `_session_keyed_tables` enumerates every session-keyed family from the catalog (the hand list had left the interactive topology — `node_state`, full-frame per tick, the biggest rows — neither exported nor purged); (2) **1-live-session retention ENFORCED IN CODE** at campaign boot: every other live session exports (fail-closed verified) then purges, archives under `~/.local/share/babylon/archives/<session_id>/`; a purged campaign keeps its catalog replay identity + parquet history (the rebuild seam); (3) **disk preflight** (new 54th GameDefines category `persistence`, 10 GiB budget) aborts the boot with a player-actionable message instead of a Postgres ENOSPC PANIC forty hours in, and the **mid-run soft warning** fires at checkpoint cadence — an attribute + a log line, never an event (disk state must not perturb any deterministic surface); (4) the **baseline ceremony** `blessed(ruling32-persistence-defines)`: the new category moves `canonical_defines_hash` — pure III.7 input-hash drift, 2 cells per baseline file, zero data movement, and the gate caught it exactly as designed.
- **#382's ledger after tonight**: rulings 28, 32, 33 fully implemented; 29 (ref_digest re-key) is the remaining near-term workforce item; 30/31/34 are Phase-4 design consequences.
- **Next**: the density/NarrationEnvelope lane — the Standard's next sequenced phase (the projection-layer density fixes + the NarrationEnvelope schema + goldens), with ruling 29 as the persistence tail.

## 2026-07-30 — Iteration 9 (part 1): the NarrationEnvelope exists, golden-pinned

- **The contract the dossier said was missing now exists** (`babylon.projection.narration_envelope`): one canonical JSONL record per COMMITTED tick — folded severity (max across the tick's events via `resolve_severity`), the bulletin summary (the loudest event's own chronicle line; quiet ticks say "no events" honestly), per-event lines, player acts, numeric deltas (the tick-summary row the session already computes), and the `entities[]` proper-noun dictionary over a DECLARED payload-key scan. Serialization = compact + sorted keys (the ContentDigest convention), so identical ticks are identical BYTES. **This is the artifact the Rust engine must emit — P27 Phase 4 gates on it (closes BSL OQ-32), and specifying it Python-first means the goldens exist before the port.**
- **Emission is live**: `GameSession.advance_tick` emits post-commit through the `narration_sink` seam (threaded like the narrator); `babylon play` appends to `vault/<campaign>/narration.jsonl`. No sink = byte-identical pre-envelope path.
- **Goldens ceremony `blessed(narration-envelope-goldens)`**: a fixed-identity Wayne boot advanced 3 ticks byte-pins the estate (`tests/baselines/narration/wayne_3tick.jsonl`) — drift is either an engine behavior change (ceremony territory) or a deliberate envelope wire change, never noise. Proven across processes.
- 1263 tests green (game+projection+cli); qa:regression 11/11 + determinism leg; vault byte-gate green.
- **Next (part 2 of the lane)**: Units A–D — the CROSSING derivative-keyed warning tier (autopause load 26/65-critical is the target), classify the 37 unclassified event types, the adaptive card ceiling, narrator beats reading axis_progress/FieldDerivative deltas on quiet ticks.

## 2026-07-30 — Iteration 9 (part 2): the autopause census drops 26 → 16, every survivor a genuine entry

- **TERMINAL_APPROACH exists** — the deliberate change to the day-one pure rule ("no warning tier for a CROSSING"): a third proximity for crossings whose own direction indicates movement TOWARD a terminal-adjacent configuration without entering it, deriving to `warning`. Ten crossings reclassified, each with a grounded drift rationale in the module's own governance registry (escapable doctrine traps, recruitment increments, reversible mode shifts, recurring market corrections, unrest below the rupture calculus, axis movement). **NOT moved: BIFURCATION_THRESHOLD** — its dependent PATTERNs (RED_SETTLER_TRAP_DETECTED, DUAL_POWER_ACTIVE) are verdict-surface and patterns ARE the verdict (Standard §2); the routing moment is the fork itself.
- **The autopause target hit**: critical census 26 → 16 (16/20/29 across the tiers), pinned so the next move is a decision, not drift. Every kept-critical member is a real axis entry/lock — nothing terminal was silenced.
- Byte-gates: qa:regression 11/11 + determinism leg; vault byte-identical; the narration goldens unchanged (no reclassified type fires in Wayne's first three ticks). 1269 model + 1241 downstream tests green with every pin moved consciously.
- **Next (part 3)**: Unit B (real EventKindRows for the classified-but-rowless remainder), Unit C (the adaptive card ceiling), Unit D (narrator quiet-tick beats reading axis_progress/FieldDerivative deltas — rulings 26/27 bind register).

## 2026-07-30 — Iteration 9 (part 3): zero wire-reaching unclassified — the loud floor now means exactly one thing

- **Unit B landed**: 17 grounded EventKindRows for the wire-reaching remainder. The unclassified→warning floor now holds ONLY for the 18 never-wire members (the fallback-coverage ledger — events that cannot arrive), where it is correct forever: a flagged-unclassified event in a live campaign is now always news, never noise. Six rare axis entries (civil war, sovereign collapse, the terminal decision, faction victory, control-ratio crisis, dual power) land critical deliberately — once-per-campaign moments the player must see; census 16 → 22 with the distinction documented (the density law governs FREQUENT criticals, not historic ones). Five approach/flow warnings, six informational. Taxonomy 82 rows; every pin moved consciously (including renaming a 47-named pin that had silently rotted to 65).
- **Second narration-golden ceremony** `blessed(unit-b-severity-reclassification)`: the reclassified tiers surface in Wayne's early ticks — same events, same deltas, only severity strings moved; the golden estate caught it exactly as designed, twice in one night.
- 2513 tests green; byte-gates green.
- **Next (part 4)**: Unit C (adaptive card ceiling) + Unit D (narrator quiet-tick beats over axis_progress/FieldDerivative deltas); then the verb 3×3 + G3 lane (Standard first-moves item 3).

## 2026-07-30 — Iteration 9 (part 4): the density lane COMPLETES

- **Unit C — the adaptive card ceiling**: the flat 1-informational-per-tick cap starved quiet ticks of the only content they had. Now deterministic-adaptive: loud ticks (any critical/warning present) keep the tight cap; quiet ticks admit 3 informational cards. Slow structural background stays legible without loud ticks losing focus.
- **Unit D — quiet-tick narrator beats**: `_narrator_beat` reads the tick-summary row the session already computes; an eventless tick's prompt carries the DECLARED structural keys (consciousness, wealth, crisis share, price/fictitious logs, imperial rent, solidarity census) — "structural drift only: …" — deterministically, absent keys never fabricated, undeclared keys never leaking. The register (rulings 26/27) stays the LLM layer's concern; this is the substrate it reads.
- **The density lane (Standard first-moves item 2) is DONE across four PRs**: the NarrationEnvelope estate (#394), TERMINAL_APPROACH + census 26→16 (#395), the wire-reaching classification + census 22-with-distinction (#396), and this part. Two golden ceremonies + one transcript regeneration caught every drift by design.
- **Next**: the verb 3×3 + G3 lane (first-moves item 3) — DRAFT the Article V matrix for Director RATIFICATION (#378's own protocol; never self-ratified), charter the G3 cost/efficacy lever behind it.

## 2026-07-30 — Iteration 9 WRAP: the density lane closed; the verb lane opens at the Director's desk

- **#397 MERGED — the density lane (Standard first-moves item 2) is complete across four PRs** (#394–#397), closed with evidence on #380/#381 and board issue #399. Fourteen PRs merged this session.
- **The verb lane opened per its own protocol**: **#398** drafts the Article V 3×3 for Director RATIFICATION — nine registered resolvers assigned to the nine cells with two structural findings surfaced honestly: Build-org × Population holds TWO verbs (EDUCATE + PROPAGANDIZE, with the Iskra argument stated), and Manage-resources × Organization is EMPTY (no verb replenishes budget or mints PRESENCE — the dossier's Wall 4), with the Director's own rulings 14/38 (the funding doctrine-pair surface) named as the chartered filler. Options a/b/c posted. **G3 (the cost/efficacy lever) is chartered BEHIND the ratification and will not be implemented until ruled.**
- **Next unblocked lane** (never stall on a pending ruling): **spatial/Phase 0-D** (first-moves item 4) — the three sha-pinned build products (land/water mask retiring the inert coverage_pct; block-group → H3 res-7 population apportionment; LODES WAC per-cell workplace density) under ruling 18's DATA FIRST, plus the two-spatial-grain reconciliation.
## 2026-07-30 — Iteration 10: the fabrication is now a measurement

- **Phase 0-D chartered and product 1 BUILT** (`feat/phase0d-charter`): the spatial data charter (three sha-pinned build products behind a declared acquisition, ruling 18's DATA FIRST), the 86-input fetch manifest (TIGER 2023 AREAWATER ×83 + block groups + LODES8 WAC/crosswalk — pinned URL+sha each; Census 429 rate-limiting survived with the backoff lesson baked in), and **the res-7 land/water mask**: 45,572 cells, each boundary polygon intersected with its county's AREAWATER union in equal-area EPSG:5070.
- **The measurement validates the dossier exactly**: 40.0% of bridge-county cells are water-dominated (the dossier predicted ~40% from area arithmetic alone); 33.7% are pure open water — the Great Lakes; mean land fraction 0.597. Against `coverage_pct` uniformly 100. The fabrication the charter existed to retire is now a registered artifact (`h3_res7_land_mask.parquet`, sha 4e6caba2…, ADR098 register mode).
- **Next**: products 2 (block-group population apportionment through the mask) + 3 (LODES WAC workplace density) — the inputs are already fetched and pinned; then the hydrator's "uniform within county" retirement.

## 2026-07-30 — Iteration 10 WRAP: ruling 18's data exists; the pacing prerequisite opens

- **PR #401 MERGED — all three Phase 0-D build products, measured and registered** (board #402): the mask (40.0% water-dominated — the dossier's independent prediction validated to the decimal), block-grain population (self-verified against Michigan's published total, 99.90% in-tiling), the workplace share key (3.93M jobs). Two acquisition lessons baked into the toolchain as code: Census 429 backoff, and the DHC API's key gate (swapped to the keyless P.L. 94-171 bulk — the pinned manifest caught the error-page immediately because a pinned artifact that isn't what it claims fails loudly).
- **The fabrications named by the dossier now have measured replacements**; consumer wiring (the hydrator reading mask + share keys) is design-gated on the P27 multi-res shape and tracked on #379.
- **Iteration 11 chartered: STANDING ORDERS** (ruling 22) — the pacing model's prerequisite, not a convenience: without verb persistence, 5,200 ticks = 5,200 forced selections and the 30-hour floor is unreachable. Declared deterministic repetition with material interrupts, in the session/pacing layer, coefficient-free per ruling 24.
- Session tally: **16 PRs merged**; lanes complete: sentinels, density, spatial data; verbs await #398's ratification; rulings 25/28/32/33 + 18 (data half) + 12 (projection half) implemented.

## 2026-07-30 — Iteration 11: the verb persists

- **STANDING ORDERS live** (ruling 22 — the pacing model's prerequisite, per the dossier's own finding: without persistence, 5,200 ticks are 5,200 forced selections and the 30-hour floor is unreachable). An order re-submits through the SAME pending-turns path a hand verb takes (zero new resolver surface, byte-identical mechanics); a fresh player verb suppresses without canceling; interrupts are material and deterministic — target-gone and autopause — never a cooldown coefficient (ruling 24); every interruption surfaces legibly. Session-lifetime in this cut (store persistence lands with the client's orders UI).
- Poetic confirmation: the first red run failed because Wayne's tick 1 fires a REAL autopause that canceled the test's order — the interrupt worked before its own pin existed.
- Byte-gates green. #380's ruling-22 consequence lands with the PR; remaining #380: the three restoration channels (ruling 23).

## 2026-07-30 — Iteration 12: the narrator can no longer lie about facts, and its voices are the Director's

- **The production grounding filter** (Standard §5; rulings 26/27's guardrail): narration may use ONLY the proper nouns and numbers its grounded inputs supplied — deterministic set arithmetic over tokens, no model in the validation loop. An invention rejects through the cache's EXISTING degraded machinery: a visible `{absence}` page NAMING the offender ("grounding filter: invented proper noun 'Chicago'…"), retryable, never a tombstone. Threaded end-to-end: `GameSession` builds the context from the tick's own events + summary row + tick number → `NarratorSideProcess.schedule` → `NarratorCache.narrate`.
- **The register pins** (rulings 26/27 as PROMPT DATA, III.12): the wire (`corporate_system`) gains the ruled flattery clause — a failing reformist line praised as responsible stewardship, dramatic irony the dossiers contradict; the underground (`liberated_system`) speaks theory BLUNT — imperial rent, the labor aristocracy, the settler bargain, pinned against model liberalization; the **Bondi Algorithm voice minted** (`bondi_system`, from observer-layer.yaml's DESIGN-ONLY spec) for carceral surfaces; and the §5 constitutional repair — `default_system`'s "game master"/"escalate or de-escalate contradictions" adjudication retired (contra Amendment V). 12 byte-checkable pins hold the ruled phrases against every future prompt edit.
- Register SELECTION mechanics (press organ, cadre correspondents, carceral routing) ride a later train; the content law now precedes the selector.
- Remaining on #381: the four-tier narration ladder (bulletin/dispatch/chapter/Book) consuming the envelope estate.

## 2026-07-30 — Iteration 13: the ratification lands, and the Director opens the archives

**Part 1 — #398 ratified and implemented.** The Director ratified the Article V 3×3 **as drafted** in a live session: the Iskra double cell stands (Build-org × Population holds BOTH `educate` and `campaign` — agitation and propaganda are distinct practices) and Manage-resources × Organization stays **honestly empty** until rulings 14/38's funding train. Now canonical frozen data (`src/babylon/game/actions/matrix.py`) under a 9-pin sentinel: moving a verb turns a gate red. **ADR177** records the session's four rulings; #398 closed with evidence (PR #406). Same session: the **branch-protection split** — dev stays fast-iteration under manual merge discipline, main became "the whole ordeal" (PR required + the 9-check battery **including the Postgres Integration Tier**, the exact check #392 slipped past). Ruling 23 (restoration channels) ruled **spec-first with Director review before code**.

**Part 2 — the research program.** The Director opened three corpora (marxists.org 20G, ProleWiki Exports 54M, the MIM etext archive 2.9G, weighting **MIM as the aligned line**) plus a counterinsurgency/police PDF library, and commissioned three investigations. Sixteen agent sweeps across three waves, every finding carrying a corpus path:

- **`reports/funding-verb-historical-dossier.md`** (69 practices) — grounds the empty cell: the RSDLP's three-way expropriation split, the IWMA levy repriced 3d → ½d, Chinese base-area org stores at 45–48% of running costs, and §1.6 MIM. Seven MIM-line tensions, headed by **T1: core-raised mass money is redistributed Φ, so the "clean" channel and the bribe are materially the same substance.**
- **`reports/organizational-methods-dossier.md`** (51 verified paths) — the mass line read as an **allocation loop whose first arrow runs upward**; Peters' 1935 structural thresholds; the 1930 CPUSA language-work audit's cadre-efficiency ratios (1:10 controlled, 1:175 influenced) with a named over-saturation waste region; and the corrective that **security is not monotonic in secrecy** — under isolating repression, spend on visible outside linkage.
- **`reports/heat-system-dossier.md`** — the Director called this **vitally important** and the finding matches: **the Okhrana drew network diagrams.** The Savinkov two-foot-by-three typed ego network; the Riga liquidation plan's "76 names in some 30 units"; an SR org-plan better than the SR Central Committee's own. Serge's production function — repression works when it "completes the effect of efficient measures of general policy" — composes out of Φ and the reform ceiling. Backfire is **the boundary of the cut**. Proposal: split heat into dossier/capacity/exposure, with indiscriminate violence firing when legibility is LOW.

**Two findings that change engineering, not just theory:** (a) **verb cost is dead code four times over** — four cost tables, zero production debits, so G3's cost half is "make cost real for the first time," not "add a multiplier"; (b) **the repression estate is already written and uncalled**, inert only because no production writer creates an org-to-org SOLIDARITY edge — inducing that graph from MEMBERSHIP + PRESENCE co-projections makes it all live with zero new math.

**Sourcing discipline held.** Serge's book is **primary-verified in full** (the three files missing from the local mirror fetched directly, per the Director's "read the entire book"). That pass re-cited the diagram material to its real chapter, verified five casualty figures — and **withdrew one claim** whose design inference had already grown around it, recording the withdrawal in place as a worked example of the hazard.

**Five director-gates filed** (#407 funding · 7 calls, #408 organizational surface incl. W-H · 8 calls, #411 heat · 6 calls), plus the two ruled gates closed. 21 PRs merged. **Nothing from the research is implemented or ratified** — the reserved calls come first.

## 2026-07-30 — Iterations 14–16: the Rust engine gets real code, the gate gets eyes, the map gets edges

**Iteration 14 — Lane C, `babylon-graph` materialized (PR #415 MERGED).** P27 Phase 1 Task 11's drafted `GraphSubstrate` trait is now compiled, tested code: the native-hyperedge trait (Amendment D — two typed halves in one substrate, `NodeId`/`HyperedgeId` distinct on purpose, no method anywhere that expands a member list into pairwise edges) plus a placeholder implementation for Tasks 16/17 to typecheck against. `a_hyperedge_mints_no_pairwise_edges` pins VIII.9's arithmetic as a test, not an argument. The drafted code would have failed the gate as written (`clippy::pedantic` + `-D warnings` demands `# Errors` docs it omitted) — fixed, deviation recorded.

**Iteration 15 — Lane B, the tick content hash (PR #416 MERGED).** The dossier's R1: `determinism_hash` is three scalars and structurally blind to a dropped node, lost edge, or corrupted attribute. `babylon.kernel.tick_hash` implements the determinism contract's P27 byte layout with executable goldens — the rewrite-test artifact — and `qa:regression` now carries one `graph_hash` per checkpoint, compared exactly, covering ~30 attributes per node plus node/edge TYPES against the dense columns' hand-picked ten. Proved live by corrupting a baseline (diagnostic FAIL) before trusting it. **Implementing the spec found three missing rules and one self-contradiction** (the worked example quoted a boolean its own rule says is bare; corrected 248→246 bytes with the superseded digest recorded); hashing real graphs found **a second live instance of the D-4 shape** — `legal_authorities` is a frozenset ON A NODE. Purely additive ceremony `blessed(graph-content-hash-field)`: one key added, 77 cells, ZERO pre-existing values moved — verified, not asserted.

**The Director ruled the reserved topology questions live (ADR179).** T1: ADJACENCY derives NOW, FULLY LIVE (overriding my shadow-first recommendation). T2: delegated with a no-debt constraint → both hashes survive with honest names (`determinism_hash` → `replay_identity_hash` + new `content_hash`; III.7 repointed — a constitutional edit on its own PR). T3: babylon-graph CONSUMES hypergraph-rs, with her explicit caveat it may not be one-for-one vs xgi and may need development — an investment decision, so the trait is now an insulation layer and Phase 2 opens with a written capability delta. T4: topology persists as a DEDICATED Postgres object, ideally Apache-AGE-queryable — superseding every option I offered (AGE 1.6.0/PG17 verified; PostGIS coexistence is the unverified risk).

**Iteration 16 — Lane D, the ADJACENCY producer (PR #417).** TIGER geometry → content-hashed artifact (9,477 pairs, the Census file's magnitude; `intersects ≡ touches` verified, zero overlapping interiors) → hash-verifying loader → bridge seeds territory↔territory edges → **spillover made symmetric** (one stored edge spills both ways; the directed-only walk would have silently halved the physics along an arbitrary FIPS axis the moment a producer existed). Readers activated: heat spillover, contiguity primitives, the bifurcation ceiling. Wayne County's four land neighbors are a golden. Zero drift on every gate that can run; the bridge-built goldens could not be exercised — `qa:e2e-regression` fails at tick 0 **on clean dev** (known spec-088 S3, verified by stash/run/pop) — so ADJACENCY's physics drift surfaces when that gate is repaired and owes its ceremony there. **Scar pinned in code:** the 3-digit `county_fips` column is only unique within a state; keying on it collapsed 9,477 pairs to 4,905 under set-dedup. The generator now refuses non-unique keys.

**Standing after 16:** 24 PRs merged this session. Lanes B/C/D of the topology program complete pending #417's CI; Lane A (heat) HELD on director-gates #407/#408/#411; next unblocked: the T2 rename sweep, the narration ladder (#381 remainder), and the spec-088 S3 e2e repair now load-bearing for two declared ceremonies.

## 2026-07-30 — Iterations 17–18: the names stop lying, the Constitution names its hash, the kernel gets arithmetic

**Iteration 17 — the T2 rename sweep (PR #418 MERGED) + Constitution v3.0.1 (PR #420, HELD for Director veto).** Three unrelated SHA-256 values wore the name `determinism_hash`; each now carries its honest name: `replay_identity_hash` (lineage, no world state — migration 0044, rerunnable information_schema-guarded, partition-cascade), `hex_frame_hash` (the audit-scoped partial content hash, `compute_hex_frame_hash()`), and `content_hash` (the P27 full-graph digest, T2's ruled name, renamed in the regression checkpoints by a zero-value-drift key-rename ceremony `blessed(adr179-t2-key-renames)` — verified structurally, 0 value diffs). Owner-queue item 31 closed; Known Discrepancies 1 & 2 marked RESOLVED with history kept. **One scar:** the severity-anchored kwarg fix missed the single ConservationAuditRow in the estate with a comment between `severity=` and the hash kwarg — caught by the PG tier (the only place that file runs), then closed properly with a constructor-SPAN scan over all three models in both directions: exactly one site, that one. The Constitution edit (III.7 names the content hash as its hash, the replay stamp as NOT it) rides PR #420 with an explicit Director veto note — constitutional text stays hers even when the ruling authorizes it.

**Iteration 18 — the Rust-first inversion, prompted by the Director ("why not just ditch the python").** Answer recorded: that IS Amendment AE; the morning's Python work was contract-authoring (goldens, reference physics, schema), not Python investment; and the queue inverts from here — new capability lands Rust-side, Python changes only to repair the frozen reference or author contracts. Proposed on #411 that heat become the first Rust/BSL-native mechanic. Executed immediately:
- **Task 3 (PR #419): kernel scalars.** `quantize` ported byte-for-byte (conformance vector verified against LIVE Python first), six bounded sorts with the Gatekeeper order pinned at the 1.0000004/1.0000005 boundary, `Ratio`'s grid-underflow rejection, and `Currency` i128 with the four pinned operators — including a REAL half-even i256 division where **the plan's draft had a truncation bug** (`/` on I256), and an integer-numerator multiply pinned beyond 2⁵³. The Step-5 cross-check found the contract pinned Currency's hash encoding but not its operator algebra — closed with a normative chapter whose worked examples are simultaneously the unit tests.
- **Task 4 (branch pushed): the deterministic sim clock.** `uuid4()`-per-tick replaced by a pure function of `(session_id, tick)`, zero-padded to sort lexicographically in tick order; `advance()` is checked, not wrapping.

**Also this block:** the Director ruled the spatial-adjacency **lookup estate** (invariant substrate → per-resolution static tables + indices, never per-tick state; H3 needs no table at all — `grid_disk` is algorithmic; Rust side compiles to a CSR at startup) — recorded on #414, first Postgres brick chartered as T4 brick 1. The memory index was compacted below its ceiling.

**Iteration 19 — the kernel ladder runs (Tasks 5–7), and a ruling catches its own violation.** Task 4's clock merged (#421), then Task 5 (PR #422): `ChaCha8Rng` under R8's stream-change license, seeded by a SHA-256 derivation. **The scar that matters:** the first cut was one stream per `(session, tick)` — exactly the shape ADR176 ruling 20's rider supersedes ("landing before P27 Phase 1 Task 5 freezes"), discovered while cross-checking Task 8's ruling status. Under LOD, one added carrier would have shifted every later draw that tick — the butterfly generator the dossier's §6.3 names. Amended in place before the freeze: one stream per `(session, tick, domain, stable_key)`, length-prefix framing so `("ab","c")` can never collide with `("a","bc")`, grain-invariance pinned by a test where a carrier's draws are byte-identical whether or not a second carrier ever draws, and **no tick-global constructor exists** — the superseded shape is unreachable by API. Tasks 6 (the deterministic event bus: four ordering guarantees as tests, blocked events log the ORIGINAL payload) and 7 (`ContentDigest`: the defines half, conformance-pinned against a real 25,184-byte Python canonical-JSON fixture — with the end-of-file-fixer hook's appended newline handled explicitly, not accidentally) rode a stacked branch; 46 kernel tests green across the chain.

**Task 8 discovered pre-ruled.** ADR176 r21 already decides poly-vs-libm: pinned soft-float libm crate, golden vectors per intrinsic. The owed analysis artifact (`ai/_inbox/sigmoid-ruling-p27.md`) was written from a fresh call-site census cross-checked against the proscription audit: the surviving tick-time intrinsic set is **`{exp, log}` at most, possibly empty** — sigmoid/tanh/entropy are never registered, so the typechecker itself keeps the proscribed forms from re-entering as content. Task 18's exit-gate precondition (ruling in hand) is satisfied.

**Standing after 19:** 29 PRs merged this session (#418, #419, #421, #422 included). #420 holds in the Director veto window; #423 (Tasks 6+7) opened off the stack, in CI at this writing. Next: the BSL reader chain (Tasks 9–10), then Tasks 12–17 per dependency order. Lane A (heat) still HELD on #407/#408/#411; the narration ladder (#27) and T4 brick 1 (#37) queue behind the kernel run per the inversion.

## 2026-07-30 — Iteration 20: the Director rules everything at once, and the language becomes machinery

**The gate-clearing session.** The Director asked for every open decision gate at once ("hit me with the decision gates you need") and ruled **all 24 calls** across six batches in one sitting — recorded as **ADR180 (R1–R20, PR #427 MERGED)**, with her words verbatim where they carry the line. All four director-gates closed with evidence (#407 funding, #408 organizational surface, #411 heat, #414 topology Q4/Q5); **Constitution v3.0.1 merged** (#420) out of its veto window; **Lane A (heat) formally unblocked** as the first Rust/BSL-native mechanic. Five rulings went **against my recommendation** and are recorded as such: the COMPETITION opposition gets a chartered producer (not retirement), the lumpen surface is a MIM(P) *modifier* (not a held question), informant flip is escapable at ruinous cost (not irreversible), W-H is a doctrine-identity property (not a stance), and the armed-struggle switch charters now. One question's premise was rejected outright with a directed reading: **"there is no peasantry in the united states, read MIM particularly the FLPMIMP document."**

**The directed reading was executed, not just noted.** FLPMIMP was absent from the local marxists.org mirror (it stops ~2008, before MIM(Prisons) existed) — the 2nd edition (March 2023) was fetched from prisoncensorship.info with provenance and **read**. It confirms the tenancy ruling directly (the glossary places the peasantry among exploited classes "almost wholly found in the Third World") and adds a **binding nuance to R9**: the forefront thesis names *prisoners* and the oppressed-nation lumpen — the First World lumpen as a whole "receives more material benefits from imperialism than the global proletariat" — so the modifier keys on imprisoned and oppressed-nation lumpen surfaces, never a blanket lumpen bonus. Recorded in ADR180's grounding note. The R20 corpus repair rode the same pass: Mao's 1943 mass-line essay and WITBD Ch. IV fetched with provenance (`/media/user/data/babylon-data/corpus-additions/2026-07-30/`), and an independent verification confirmed **both dossier arguments survive the primary texts** — the "endless spiral" is verbatim-accurate, and Ch. IV sharpens one mechanic: the secret core and the broad periphery operate *simultaneously*, not sequentially.

**The BSL ladder ran four rungs while the rulings landed (Tasks 9, 10, 12, 13).** The standing lesson held every time: *the plan sketch was wrong against the normative doc on all four tasks*, and the normative text governed.

- **Task 9 (PR #425 MERGED) — the reader.** The full §1 lexical grammar: maximal-munch-then-classify, one `E-LEX` code per rejection class with accepting+rejecting vectors, literals canonicalized at lex time, iterative parse so hostile nesting can never overflow the stack. **Implementing it found a spec self-contradiction:** §1.4's atom table omits the ten operator tokens that §2 quotes as terminals and §5.2 lists as form tags — by its letter the reader must reject the spec's own §5.6 worked example. Repaired with a distinct operator atom class (exact-match closed set, checked before the numeric path so `-` is an operator but `-5` stays an int) and a draft-ruling note recorded in §1.4.
- **Task 10 (PR #426 MERGED) — the §3.4 intensivity typechecker.** The five-row per-operator law as a compile error: sum of an intensive field is E-TYPE-041 *even weighted*, unweighted mean of an intensive is E-TYPE-042, a non-extensive weight is E-TYPE-043, min/max always legal, count returns Int — the variance-error class caught by the type system, exemptions suppressing per named field only. The plan sketch got the table wrong twice (rejected min/max; let a weight rescue sum) and its own test used symbols the grammar forbids.
- **Task 12 (PR #428 MERGED) — canonical AST serialization.** The §5 binary CAS byte-for-byte: the spec's own worked example reproduced **all 421 bytes and both pinned digests on the first run**. `rules_hash` became MANDATORY on `ContentDigest` (the empty-set hash pinned independently on the kernel side — computed, never invented). A test-only decoder proves self-delimitation.
- **Task 13 (PR #429, in CI) — the load-time fuel bound checker.** §3.7's claim made machinery: `bound(rule) = cost(when cond) + Σ cost(effects)`, rejected as E-LOAD-040 at content load — Power-of-10 Rule 2 as a static property of content. The §5.6 anchor computes **bound = 7 exactly**, proving bindings never enter the bound. Per-query-head ceiling-axis dispatch (`members-of` → `:max-members`, the Amendment D member-count axis; everything else → `:ceiling`); a missing ceiling is a loud error, never a silent 0; E-LOAD-042's static member-list check included; saturating arithmetic so an overflowed bound still fails loud. One normative completion recorded in §3.7: the checker charges a query's operand expression — §4.5 charges every evaluated node, and omitting it would make the static bound under-count the runtime meter.

**Standing after 20:** **36 PRs merged this session**; #429 in CI. The kernel ladder stands at Tasks 1–13 complete (of 18); remaining: 14 (fuel evaluator), 15 (bindings + `:material-basis`), 16 (typed structural verbs against babylon-graph), 17 (conformance corpus), 18 (exit gate — its ruling precondition already satisfied). Lane A implementation (#32) is next after the ladder per the inversion; the narration ladder (#27) and T4 brick 1 (#37) still queue behind it.

## 2026-07-30 — Iteration 21: the ladder tops out — Phase 1 of Program 27 is complete

**The kernel ladder's last four rungs (Tasks 14–17), each implemented from the normative doc against a wrong plan sketch — the pattern held to the very end.**

- **Task 14 (PR #431 MERGED) — the fuel-metered evaluator.** §3.3's value lanes as a Rust enum, §4.3's arithmetic codes as structured tick-aborts: checked i64 (overflow = E-EVAL-011), Currency through the kernel's four pinned operators with their panic preconditions pre-checked into E-EVAL-010/012/013, always-finite binary64 (E-EVAL-014), and **exact IEEE equality where the sketch wanted an invented epsilon** — the spec pins bit-exact comparison, so an epsilon would be new semantics minted by an implementer. §4.5's per-node charging runs through one shared meter with both sides of the E-EVAL-040 boundary pinned by test (the §5.6 condition consumes exactly 2). Two spec gaps found and recorded as draft notes: the §3.7/§4.5 **off-by-one** (a rule whose bound equals its `:fuel` loads and then dies at runtime — budget `:fuel ≥ bound+1` until the review rules), and `Int ÷ Int` having no pinned semantics (a loud error today, never a silent truncation choice).
- **Task 15 (PR #432 MERGED) — bindings + `:material-basis` + the `:default` allowlist.** All four bind sources, every named code (E-PARSE-011/012/013/022/030/031, E-LOAD-010/011 — an unregistered metric is a load rejection, **never 0.0**), and §3.5 item 4 implemented as written: a `:default` outside the migration corpus's allowlist is a **lint finding requiring Director sign-off, not a load error** — the allowlist is program state, not language state. The E-LOAD-010/011/030 per-source assignment ambiguity is recorded in the module doc for the review.
- **Task 16 (PR #433 MERGED) — the typed structural verb algebra + modding anchors.** All seven verbs + `emit` executing against `GraphSubstrate` through the same fuel meter; guards charge and gate; the §3.3 store boundary is E-EVAL-020, **never a clamp**; E-EVAL-031 existence discipline; roster change is remove+add in one effect list (whole-hyperedge replacement, the ruled §2.8 shape); VIII.9's no-clique-expansion pinned again at the executor level. Three **honest trait completions** recorded in babylon-graph's module doc rather than smuggled: `&str` type/attribute parameters (the `Box::leak` interning shape was rejected — no leak call exists in the workspace), `add_edge` carries `:strength`, and `node_attribute` reads LOUD on an unwritten attribute — never a fabricated 0.0. One draft ruling recorded in §2.8: id operands are effect-list-scoped names. Anchors: E-LOAD-002 (a rule landing nowhere is a load error); total-order resolution + E-LOAD-003 deferred to babylon-engine by name.
- **Task 17 (PR #434 MERGED) — the conformance corpus + the composed pipeline.** Both Python corpora — 899 lines — **read end-to-end before transcription**; all four M8 correction sites re-verified at their exact cited lines (zero drift) and transcribed as explicit correction tests: silent-`True` empty preconditions → loud `(when)` rejection, unknown-metric-reads-0.0 → E-LOAD-011, unknown-comparison-falls-to-False → E-LEX-003, unknown-aggregation-falls-to-False → loud typecheck rejection. The **composed `rule_pipeline::load_rule`** is the first point where every load gate composes, in §4.6 class order, plus `bind_environment` (the §3.5 evaluation half — required-missing is loud, `:optional` takes its declared default, no rule ever observes absence). The `DEFAULT_ALLOWLIST` populated with its six governed rows. A bonus the §3.4 law caught: Python's `sum_strength`/`avg_strength` **silently commit the recorded intensive-aggregation variance error** — E-TYPE-041/042 here, pinned by test. The bifurcation resolution routing executes **both directions against a real substrate**. Delta ledger: `reports/p27-conformance-corpus-transcription.md` — every Python test row carries a disposition (executes / load-level / out-of-BSL-scope with its named home).

**Task 18 — the exit gate (this PR).** The verification battery ran green with **zero errors on every leg**: workspace tests, workspace clippy `-D warnings`, per-crate pedantic clippy on all three crates, `cargo fmt --check`, rustdoc `-D warnings`. `docs/reference/phase-1-exit-checklist.md` records the DONE table (Tasks 1–17 with their PR numbers), the DEFERRED table (every deferral named, owned, never silent), the two flagged TODO dispositions (no leak shipped; the composition BUILT), and the draft-rulings register for the Phase-1 review. **Phase 1 (Language & Kernel) is COMPLETE at this document's merge.** Phase 2 (Content & Intrinsics) opens with no outstanding constitutional gate: Amendment D ruled, ADR176 r21 in hand, ADR180 clearing the board.

**Standing after 21:** 42 PRs merged this session with #434; this exit-gate PR closes Phase 1 at its merge. Next per the inversion: **Lane A heat (#32)** as the first Rust/BSL-native mechanic under ADR180 R3–R7; the narration ladder (#27) and T4 brick 1 (#37) queue behind.

## 2026-07-30 — Iteration 22: Phase 0 closes after Phase 1, the Director audits the auditors, and Copilot's ignored reviews turn out to hold real bugs

**Phase 0 formally closed — the freeze tag exists and executes.** The morning's Director merge of #370 (the phi_hour clamp) had silently cleared Phase 0's last blocker, but Task 17 — the `p27-python-freeze` executable pin that Amendment AE binds the Python freeze to — had never run. Executed per the plan's recipe: the four-leg precondition battery green (14,209 unit tests, the 83-test laws floor, both byte-gates, zero open director-gates), the annotated tag cut on dev `bb6df6cf` with its pin set recorded (source, `flake.lock`, `uv.lock`, the reference-DB **registry** sha per ADR098 — the worktree's local DB copy turned out to be a stale pre-P26 file, which is exactly why the registry is the canonical pin), the `frozen-engine` weekly gate landed (#436), and the first dispatch run came back **green** (run 30569880754) — the pin executes from a cold checkout. Train #343 closed with evidence. The Python engine is reference-only, provably, on a weekly heartbeat.

**The Director asked how to use Copilot/CodeQL/CI better — the audit answered with numbers.** Six agents (five read-only evidence lanes + Opus synthesis): the gate-estate map with the branch-protection reality, 400 runs of Actions history, a Copilot comment harvest with per-comment classification, the CodeQL budget, and a scar-vs-gate archaeology. Headlines, each lane-traced (`reports/ci-copilot-codeql-synergy-2026-07-30.md`, PR #437): **Copilot review is 83% substantive and 0% acted-on** — merges land before its median 230s review even posts; **CodeQL spends 418 min/day on the Python estate that froze this morning and zero on the Rust workspace that is now the engine**; Rust Gate / Ceremony Gate / PG Tier are NOT required checks on dev; **nightly has failed 76 of 76 runs ever**, burying the michigan-rollover gate created yesterday; eight recurring scar classes still live as prose. R1–R10 and the eight what-not-to-dos are RECOMMENDATIONS AWAITING THE DIRECTOR — nothing implemented.

**The verification addendum practiced what the report preaches.** Every defect claim was independently re-checked before delivery: four CONFIRMED (one claim's cited file doesn't exist — recorded as a lane transcription error; one confirmed-but-flagged, the retention XDG question, because its "fix" would touch the Director's logging directive). The confirmed four were then fixed TDD-red-first in `fix/p27-conformance-defects`:

- **`quantize` leaked −0.0** on the open interval (−5·10⁻⁷, 0) — the Python reference emits +0.0 there (live probe), and the tick hash encodes bit patterns; the module's own "never sees a quantized −0.0" claim was false. Normalized, boundary pinned bit-exact both sides.
- **`Currency × Int` slipped the §3.2 operator table** through the promoting lane — live for exactly the values 0 and 1 (the [0,1] domain check caught the rest), but the law bans the FORM at any value (`bsl-language.rst:849`). `Int` is now rejected in either operand order.
- **Two `(anchor …)` forms silently resolved first-wins** — a III.11 violation; now a loud single-anchor error.
- **A stripped BOM skewed every subsequent error position by 3 bytes** — positions are now file-byte offsets regardless.

The loop closed on itself: the audit's central finding — *the largest already-paid-for signal in the estate is being 100% discarded* — was proven by harvesting that signal once and finding four real defects on Phase 1's conformance surface, one of them on the tick hash. That is R1's argument, run live before the ruling.

**Standing after 22:** Phases 0 AND 1 of Program 27 both closed with evidence; 45 PRs merged this session with #436, #437 + the fix PR in flight at this writing. Next per the inversion: **Lane A heat (#32)**; the narration ladder (#27) and T4 brick 1 (#37) behind it; the synergy report's R1–R10 sit with the Director.
