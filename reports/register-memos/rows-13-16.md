# Director-gate register — decision memos, rows 13–16

Charter: ADR202 R11 ("register rows 9-24: MEMOS FIRST, RULED ASYNC … per-row decision memos in
`reports/register-memos/`"). Register source: issue #564, verbatim from
`reports/port-estate-survey-2026-08-12.md` §5b. Every code anchor below was re-verified against the
tree on 2026-08-14 (branch `docs/adr202-t4-rulings`); none of the four cited anchors has moved.
Nothing here is ruled by the workforce — each row ends at lettered options and a recommendation.

Cross-cutting fact: `grep` over `rust/crates/**` finds zero occurrences of `fascist`/`revanch`,
`substrate_floor`, or `colonial_stance` — none of these constructs exists in the Rust estate yet,
so each ruling is a pre-port decision about what the transcription carries.

---

## Row 13 — Community @6.0: `SUBSTRATE_FLOOR_DEFAULTS`

**Verbatim (#564):** "13. Community SUBSTRATE_FLOOR_DEFAULTS — consciousness floors by oppressed
nationality, transcribe verbatim under ADR171? (#536 input)"

**Code evidence (verified):**
- `src/babylon/models/entities/consciousness.py:356-455` — the dict (survey's `356-455` is exact;
  `__all__` export at `:460`). Fourteen `CommunityType`-keyed `SubstrateFloor` entries, each with
  `ProvenanceLevel` + `data_sources` + `computation_method`:
  NEW_AFRIKAN 0.12 (MEDIUM; Vera + Chetty), FIRST_NATIONS 0.12 (MEDIUM), INCARCERATED 0.18
  (MEDIUM; Vera), CHICANO 0.08 (LOW; Chetty), UNDOCUMENTED 0.10 (LOW, "estimated"), TRANS 0.06,
  WOMEN 0.04, QUEER 0.04, DISABLED 0.03 (all LOW, "estimated"), ELDER 0.02 (LOW, "generational
  memory"), and the zeros: SETTLER / PATRIARCHAL 0.0 (HIGH, "hegemonic default: no substrate
  revolutionary consciousness"), YOUTH / ADULT 0.0 (HIGH, lifecycle).
- Sole consumer: `src/babylon/engine/systems/community.py:453` — `SUBSTRATE_FLOOR_DEFAULTS.get(comm_type)`,
  floor 0.0 when absent, fed to `compute_ternary_consciousness(substrate_floor=…)`.
- **Not** in `defines.yaml` (verified by grep): a hardcoded module-level table, against the
  "never hardcode a coefficient" convention — though every entry self-documents provenance.
- The register's "by oppressed nationality" is imprecise: the table spans all 14 community
  types — oppressed nations, gender/exclusion categories, hegemonic zeros, lifecycle phases. The
  ideological charge concentrates in the non-zero marginalized floors and the two hegemonic zeros.

**Binding rulings:**
- **ADR171** — the cited ruling. OQ1 rules the B+C+I named-nations partition "matching … the live
  COLONIAL_AXIS CommunityTypes" (so NEW_AFRIKAN/FIRST_NATIONS/CHICANO floors sit on ruled ground).
  But OQ5: "chauvinist_pressure untouched … any national input is a future Director escalation,
  baseline-moving"; OQ9: the guard "ships BEFORE any consciousness wiring"; and "Phase 3
  consciousness wiring is withdrawn as engineering and re-filed as OQ5+OQ9." ADR171 thus names
  this exact class — national content feeding consciousness — as **hers**, not a transcription.
- **ADR198** — "AG(i) membership payloads a SEPARATE later ceremony chartered by #536."
- **#536** (verified, OPEN) — "staged charter riders: Community port train — BSL hyperedge lane
  goes live as a NAMED deliverable": the Community train's charter; attributed membership
  (Amendment AG / ADR189) is Community's semantics. Row 13's "(#536 input)" marks this memo as
  charter input to that train.
- **ADR070** (adjacent) — shadow-first phased cutover precedent for reserved content.

**Options:**
- **A. Transcribe verbatim** as content data with a D-record citing ADR171. Fastest; preserves
  byte-behavior. Cost: silence-ratifies magnitudes (0.12/0.18/0.08/…) that are workforce estimates
  at LOW/MEDIUM provenance and have never been ruled — exactly the move ADR171 OQ5 pre-empts.
- **B. Director rules the table now, then port transcribes the ruled table.** This memo presents
  every value above; she confirms or amends each non-zero floor and the hegemonic zeros in one
  async pass. Port proceeds with a clean provenance chain.
- **C. Port the machinery, floors declared absent** (NoDataSentinel-style), values deferred to the
  #536 ceremony. Maximal reserved-line hygiene; but silently zeros live frozen-engine behavior
  wherever communities are seeded (byte-drift), and inverts ADR171 OQ9's guard-before-wiring order.
- **D. B + lift the table into declared content** (`defines.yaml` / `.bscn` deffield) at port —
  convention hygiene folded in, values unchanged from what she rules. Costs one tiny defines
  ceremony.

**Workforce recommendation: B** (with D's mechanical relocation as engineering, values untouched).
The table is small and fully presentable; ADR171 already assigns this question class to the
Director. Ruling it once now unblocks the #536 train without smuggling.

**Reserved-line flags (Director only):** the per-nation floor values for New Afrikan / First
Nations / Chicano communities; the SETTLER/PATRIARCHAL zero ("no substrate revolutionary
consciousness" is a theoretical claim, not a measurement); whether LOW-provenance estimates ship
at all; any future un-zeroing (ADR171 OQ5: baseline-moving escalation).

---

## Row 14 — Allegiance @17.42 / FascistFaction @17.4 / Electoral @17.45: the fascist token list

**Verbatim (#564):** "14. The ('fascist','reaction','revanch','settler') token list hardcoded
TWICE (allegiance.py:77, reactionary.py:71) — one ideological classification, one ruling"

**Code evidence (verified — both register anchors exact):**
- `src/babylon/engine/systems/allegiance.py:77` — `_FASCIST_IDEOLOGY_TOKENS = ("fascist",
  "reaction", "revanch", "settler")` (comment :75-76: "mirrors FascistFactionSystem's
  BalkanizationFaction token fallback"); consumed at `:419-421` `_is_fascist_vehicle` — substring
  match on a PoliticalFaction's free-text `ideology`.
- `src/babylon/engine/systems/reactionary.py:71` — the identical tuple (comment :70, "D2
  fallback"); consumed at `:215-227` `_find_fascist_faction` — `settler_uphold OR token_match`,
  then `min(candidates)`.
- No `defines.yaml` entry (verified). Two further hardcoded ideological maps live in
  `electoral.py`: `_FACTION_BY_IDEOLOGY` (:139-144, `"fascist" → StateFaction.SETTLER_POPULIST`)
  and `_SPOILER_POLES` (:159-164, `"fascist" → "right"`) — the same classification in a different
  key-space (exact snake_case, not substring). So the "two homes" are really four.
- Shared bug surface with register row 9: `reactionary.py:224`'s substring match hits `"settler"`
  inside `"anti-settler abolitionism"` (FAC_DECOLONIAL mis-selection). Row 9 is separately
  escalated as not-port-as-is; whatever form row 14's ruling takes determines whether that bug
  class survives the port by construction.

**Binding rulings:**
- **ADR016** — the bifurcation direction law; the theoretical ground for what the fascist pole IS
  (ADR202 R7 re-affirmed "ADR016 untouched").
- **ADR054** (spec-071) — origin of the reactionary D2 predicate; **ADR134** (U8, the Allegiance
  valve); **ADR136** (U10, the electoral machine that owns the two maps).
- **ADR202 R6** — FascistFaction port ruled Option A, full measure at port, "routing structure
  transcribed exactly per the production port's precedent" — the classification is part of that
  routing, so its form must be settled before the R6 landing.
- **ADR196** — `OrgKind.POLITICAL_FACTION` minted into the Rust BSL vocabulary; **no**
  ideology-token or fascist-marker vocabulary exists there (verified: zero Rust hits).

**Options:**
- **A. One classification, one home, content-side.** A single declared token set (or enum) minted
  into the BSL vocabulary / defines; all four consumers read it. One ruling, one ceremony
  (ADR176-pattern mint or a defines addition). Kills the duplication; leaves substring semantics
  intact unless paired with C.
- **B. Port-as-is, twice (four times).** Zero ruling cost; cements a duplicated ideological
  classification into the Rust engine and — unless row 9 is fixed first — its bug shape too.
- **C. Replace text-matching with a declared attribute** (`is_fascist_vehicle` / ideology enum
  seeded on factions). Eliminates the row-9 substring bug class by construction; but shifts the
  epistemics from "read the label" to "declared property" — who declares it is itself an
  ideological act.
- **D. Fold into the FascistFaction port packet** (ADR202 R6 already sequences that train).
  Coherent timing for one consumer; leaves Allegiance/Electoral without an answer if they port
  first.

**Workforce recommendation: A+C** — one classification with one home (A), carried as a declared
attribute rather than a substring probe (C), so the row-9 bug class dies by construction. **What
counts as fascist is not ours to say** — if the Director wants minimal ceremony, B with the row-9
fix ruled separately is the honest fallback.

**Reserved-line flags (Director only):** the membership of the classification — which tokens /
ideologies are fascist ("settler" as a fascist marker is an MLM-TW line claim, not engineering);
whether identification is label-match or declared property; the electoral pairings
(`fascist → SETTLER_POPULIST`, `fascist → right` pole).

---

## Row 15 — Contradiction @18.0: the `national` opposition + `_STANCE_CHAUVINISM_SCORE`

**Verbatim (#564):** "15. Contradiction national opposition + _STANCE_CHAUVINISM_SCORE — confirm
restraint level" (survey: "Described, not proposed on. Confirm the restraint is the right level.")

**Code evidence (verified):**
- `src/babylon/engine/systems/contradiction.py:159-163` — `_STANCE_CHAUVINISM_SCORE`:
  UPHOLD 1.0 / IGNORE 0.5 / ABOLISH 0.0. Comment block :153-158 declares it "a hardcoded
  categorical map, not a GameDefines coefficient — the same division as
  `formulas.balkanization._STANCE_TO_POLICY`."
- Consumed at `:652` inside `_national_chauvinism_balance` (:596-657): INFLUENCES-weighted
  ratio-of-sums, returns `1.0 − 2.0·weighted_mean` (internationalism positive, matching the
  `price_value` sign convention), `None` when no faction carries stance+influence — "absent, not a
  fabricated neutral (III.11) … the permanent, by-construction reading in all 5 canonical
  scenarios" (:611-616).
- Catalog entry `src/babylon/domain/dialectics/instances/catalog.py:70-81`: `national` =
  national-chauvinism⇄internationalism (Lenin, *Imperialism and the Split in Socialism*; doctrine
  tag NATIONAL_CHAUVINISM / negation INTERNATIONALISM), "SHADOW (task #42-C) … reads absent (0,0)
  there BY CONSTRUCTION", "National; unplaced on the level lattice"; the antagonistic trio is
  reserved to `capital_labor`, `imperial`, `national` (:140).
- The restraint has three layers: (i) SHADOW, never promoted; (ii) absent-by-construction in
  canonical scenarios; (iii) a hardcoded 3-point ordinal map instead of a coefficient.

**Binding rulings:**
- **ADR171** — OQ5: chauvinist_pressure untouched, "any national input is a future Director
  escalation, baseline-moving"; OQ6: `national_oppression` stays shadow, un-shadowing is a
  §6.5 ceremony "reserved to the Director."
- **ADR070** — the shadow-first phased-cutover pattern this opposition already follows.
- **ADR202** — R9 retired the ±0.7616 balance cap on this system ("full antagonism must be
  displayable"); R0 keeps ADR188 Rows 7-8 standing — but a 3-point categorical map is not a
  stipulated curve, so the no-imposed-forms estate does not bite it directly.
- **spec-070 FR-002/FR-032** (ADR048) — the stance semantics the score ordinalizes (IGNORE = the
  RED_OGV middle).

**Options:**
- **A. Confirm restraint as-is** — port SHADOW, hardcoded 1.0/0.5/0.0, absent-by-construction.
  Zero new exposure; leaves an ideological metric (is UPHOLD exactly twice IGNORE?) riding as
  arithmetic with a comment for a warrant.
- **B. Confirm the shadow restraint; lift the score triple into declared content** (defines or
  `.bscn` defconst) ruled once by the Director. Same promotion restraint, honest numbers; one tiny
  ceremony.
- **C. Loosen** — promote `national` toward live at port (it reads non-absent once factions
  exist). Squarely against ADR171 OQ5/OQ6 unless she rules it.
- **D. Tighten** — don't port the national reading at all until the ADR171 Phase-2 chain
  (community_memberships seam) unblocks. Maximal reserve; strands the spec-070 FR-002 reading
  infrastructure and the RED_OGV middle semantics mid-air.

**Workforce recommendation: B** — keep every layer of the restraint (shadow, absence, no
promotion), but have the Director rule the 1.0/0.5/0.0 triple once as declared content rather than
inherit it from a comment. If she judges the triple already ruled by spec-070's stance semantics,
A costs nothing and closes the row.

**Reserved-line flags (Director only):** the chauvinism metric itself (the claim that IGNORE is
half-chauvinist); the pole naming (chauvinism⇄internationalism); any promotion out of SHADOW
(ADR171 OQ5/OQ6 reserves it, baseline-moving).

---

## Row 16 — FactionInfluence @14.5: `colonial_stance` as the principal-contradiction axis

**Verbatim (#564):** "16. FactionInfluence colonial_stance as the principal-contradiction axis —
confirm port-as-is"

**Code evidence (verified):**
- The axis claim: `src/babylon/models/entities/balkanization_faction.py:19` — "I.1 Settler-Colonial
  Frame: `colonial_stance` is the principal axis"; field at `:66`; `ColonialStance` enum at
  `src/babylon/models/enums/balkanization.py:39-58`.
- FactionInfluence consumption: `src/babylon/engine/systems/faction_influence.py:171-190`
  (`_emit_red_settler_trap_events` — reads `colonial_stance` + `class_reduction`, fires
  RED_SETTLER_TRAP_DETECTED) via `src/babylon/formulas/balkanization.py:215-252`
  (`detect_red_settler_trap`: ABOLISH exempt at `:250`; threshold
  `red_settler_trap_class_reduction_threshold: 0.6` **is** data-driven, `defines.yaml:347`;
  stance-default modifier tables at `defines.yaml:321-333`).
- The four seed rows (`src/babylon/data/game/balkanization/seed_factions.json`):
  FAC_RESTORATIONIST uphold / class_reduction 0.0; FAC_WORKERS_CONGRESS ignore / 0.7;
  FAC_DECOLONIAL abolish / 0.5; FAC_LIBERAL_IMPERIAL ignore / 0.7. (Under the trap formula both
  IGNORE factions at 0.7 ≥ 0.6 fire RED_SETTLER_TRAP — the seeds deliberately encode two trap
  specimens.)
- Downstream consumers beyond FactionInfluence: contradiction.py (row 15), `collapse_transition.py:306`,
  `electoral.py:425-434`, `endgame_detector.py:806`, and sovereignty `_STANCE_TO_POLICY`
  (register row 11, separately escalated).

**Binding rulings:**
- **Constitution I.1** — the settler-colonial frame; the principal-axis claim is constitutional,
  not a code comment's invention.
- **ADR048** (spec-070) — the balkanization estate that ratified stance semantics (FR-002, FR-034).
- **ADR171** — the named-nations line; `colonial_stance` is the faction-level expression of the
  national question she ruled.
- **ADR200** — the standing precedent for a RESERVED-LINE surface transcribed exactly *after*
  ruling (Amin/Wallerstein producer-role routing); the model for what "confirm port-as-is" means.
- **ADR196** — factions enter the Rust vocabulary as `OrgKind.POLITICAL_FACTION`; the stance axis
  rides that mint. **ADR202 R11** charters this memo.

**Options:**
- **A. Confirm port-as-is** — stance enum, seed values, trap formula transcribed exactly (ADR200
  precedent), D-record filed. The axis claim was ratified at spec-070's adoption and ADR171 stands
  behind it.
- **B. Confirm the axis; re-rule the seed table** — the four rows are content assertions about the
  US political field (a class-first social democracy at 0.7 class_reduction that IGNOREs the
  colonial relation *is* the RED_OGV specimen — is that her read of the current?).
- **C. Re-open the principal-axis claim itself** — is `colonial_stance` THE principal-contradiction
  axis for faction classification, or one axis beside class reduction? The workforce sees no new
  evidence warranting re-opening I.1; listed for completeness.

**Workforce recommendation: A**, executed so the four seed rows are placed in front of the
Director as the visible content being ratified (B's table is part of the A confirmation, not a
separate question). "Port-as-is" must not smuggle the seeds past unnoticed.

**Reserved-line flags (Director only):** principal-contradiction status of the colonial axis
(I.1); the four seed factions' stance assignments (an ideological classification of political
currents); RED_SETTLER_TRAP semantics (class reduction without decolonization = trap — already
ratified by spec-070 FR-034, re-confirmed by an A ruling).

---

*Prepared by the engineering workforce under ADR202 R11. All anchors re-grepped 2026-08-14;
`defines.yaml` checked for each hardcoded surface; Rust estate checked for pre-existing forms
(none). No code was modified.*
