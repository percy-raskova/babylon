# Director-gate register memo — rows 17–20

Charter: ADR202 R11 ("register rows 9-24 ruled memos-first, async rulings"). Register: issue
#564, verbatim source `reports/port-estate-survey-2026-08-12.md` §5b. All anchors re-verified
against the tree 2026-08-14 (dev); stale citations get the current line. The workforce
recommends; only the Director rules. Nothing here is a decision.

---

## Row 17 — Struggle @16.0: George Jackson bifurcation + SocialRole taxonomy

**Register row (verbatim §5b):** `| 17 | Struggle @16.0 | George Jackson bifurcation —
comprador insolvency → LA/periphery fork, `national_identity`, the `SocialRole` taxonomy |
Confirm port-as-is |`

**Code evidence (verified):**

- Trigger: `_check_power_vacuum`, `src/babylon/engine/systems/struggle.py:523-587` — comprador
  insolvency (`wealth < subsistence_threshold`, 551) → `revolutionary_capacity = organization *
  class_consciousness` (564) on `PERIPHERY_PROLETARIAT` → fork on `defines.jackson_threshold`
  (582). Position `16.0` at `struggle.py:235`.
- Arm 1, Revolutionary Offensive: `struggle.py:589-624` — `p_revolution=1.0` +
  `revolutionary_agitation_boost` on the periphery proletariat.
- Arm 2, Fascist Revanchism: `struggle.py:626-675` — `national_identity +=
  fascist_identity_boost` (652) and `p_acquiescence` boost (656) on the `LABOR_ARISTOCRACY`
  (643). The "LA/periphery fork" phrasing is accurate: the arms land on different classes.
- `national_identity` write path: `_update_national_identity`, `struggle.py:112-140` — carries
  its own two absent-data `0.5` defaults (128, 137), the same constant as row 19 (one ruling
  should sweep or explicitly exempt these copies).
- SocialRole taxonomy: `src/babylon/models/enums/social.py:12-43` — 8 MLM-TW class positions;
  consumed via `_STRUGGLING_ROLES` (`struggle.py:50-55`) and `_find_entity_by_role`
  (`struggle.py:178-211`).
- Coefficients already data-driven: `defines.yaml:285-288` (`jackson_threshold: 0.4`,
  `revolutionary_agitation_boost: 0.5`, `fascist_identity_boost: 0.2`,
  `fascist_acquiescence_boost: 0.2`). Register cites no lines; nothing stale.

**Binding rulings:**

- ADR070 (Program 19): the typology is already ruled **slots-as-positions** — seed vocabulary +
  display names, role attr never mutated; Struggle gates are a named Phase-3+ cutover site, and
  the consequences name this code: "The George-Jackson walk … get[s] one shared
  multiplicity-aware helper in Phase 3."
- ADR016: the bifurcation direction law (agitation without solidarity → fascism); ADR202 R7
  confirms ADR016 untouched by the curve estate.
- ADR196: minting names into the Rust `ClosedVocabulary` is a **ceremony act** ("every further
  edge earns its ceremony"); SocialRole is not yet minted.
- ADR202 R11: this memo lane.

**Options:**

- **A. Port-as-is, unconditional.** Verbatim mechanic, mint the 8 role names by ceremony. Risk:
  mints the seeded taxonomy into the new estate without acknowledging Program 19's standing
  demotion — reads as re-ratifying the nouns.
- **B. Port-as-is under a D-record (recommended).** Verbatim transcription; D-record cites
  ADR070 (roles = seed slots pending Phase-3 derivation) and routes the `struggle.py` `0.5`
  copies into row 19's ruling. Zero drift; the taxonomy question stays explicitly open.
- **C. Defer behind Program 19 Phase 3.** Roles enter Rust only in derived form. Cost: blocks
  the Struggle train on the deepest ADR070 cutover (ControlRatio-last ordering); years out.
- **D. Re-derive the fork as a measure.** No obligation — a threshold fork is not a stipulated
  curve; the ADR188/ADR202 estate does not reach it. Listed for completeness.

**Workforce recommendation: B.** Small, define-driven, already ruled twice (ADR016 lineage,
ADR070 demotion); verbatim + D-record keeps the Phase-3 promise visible at the new boundary.

**Reserved-line flags (Director-only):** the `SocialRole` taxonomy itself — which classes exist
and their names is the MLM-TW class partition; minting it into the Rust vocabulary is the
Director's ceremony to dispose. The fork's political content (LA turns revanchist, periphery
carries the revolutionary arm) is ratified mechanics — flagged, not re-asked.

---

## Row 18 — OODA @14.0: CLASS_ANALYSIS theory bonus + LEGISLATE proxy

**Register row (verbatim §5b):** `| 18 | OODA @14.0 | `CLASS_ANALYSIS` theory bonus;
`StateFaction`→`PolicyAxis` LEGISLATE proxy | Confirm port-as-is |`

**Code evidence (verified):**

- Theory bonus: `src/babylon/ooda/action_effects.py:99-111` (Step 7.5, "DoctrineSystem Unit 6b,
  ADR073") — `scaled_delta *= 1.0 + doctrine.theory_bonus_per_class_analysis *
  min(class_analysis, 10.0)` inside `compute_consciousness_delta` (39); define
  `src/babylon/config/defines/doctrine.py:87`, value `0.02` (max +20% at the tag cap),
  `defines.yaml:981`.
- LEGISLATE proxy: `src/babylon/ooda/npc_stub.py:465-494` — LEGISLATE survives as a params
  marker (489), routes to the policy-agenda enqueue, drafts the axis by dominant faction:
  `FINANCE_CAPITAL→war_posture`, `SECURITY_STATE→police_budget`, `SETTLER_POPULIST→border_regime`
  (476-480). Declared in-code: "the declared U9 proxy — U10's governing platform replaces it"
  (471-475).
- OODA position `14.0`: `src/babylon/engine/systems/ooda.py:101`. Register cites no lines;
  nothing stale.

**Binding rulings:**

- ADR073 (DoctrineSystem; Unit 6 wiring baseline-moving) + ADR137 (DoctrineTag pinned at exactly
  three tags; reformist tag movement via measured practice only). The bonus is a ruled,
  define-backed, corpus-grounded mechanic ("correct prioritization, theory bonus").
- ADR135 (1): the REPRESS-misstamp fix this proxy descends from; §6: overlay consumers declared.
- ADR136 (U10, landed): government formation is live, but the platform axis-drafting named as
  the proxy's replacement is not — the proxy is still the operative path
  (`policy_default_magnitude`, `defines.yaml:1097`, covers magnitudes, not axis selection).
- ADR134 (U8): sibling superstructure context.

**Options:**

- **A. Confirm port-as-is, plain.** Both surfaces verbatim. Risk: the proxy outlives its charter
  — U10 landed, its sunset clause is open, and a plain confirmation retires it by silence.
- **B. Port-as-is with one D-record (recommended).** Bonus verbatim (ruled twice, define-backed,
  no imposed form — outside the curve estate); proxy verbatim plus a D-record renewing the named
  replacement path so the stopgap cannot fossilize.
- **C. Refuse the proxy until platform axis-drafting exists.** Blocks the whole superstructure
  producer loop (agenda → overlays → row 20's valve) and cascades into ADR135's ruled read-sides.
- **D. Re-derive the bonus.** No obligation: a multiplicative tag-scaled delta with a define is
  not a stipulated functional form.

**Workforce recommendation: B.** Both surfaces are already-ruled constructs with named
paperwork; the only defect is the proxy's unenforced sunset, and a D-record is the instrument.

**Reserved-line flags (Director-only):** the faction→axis map is a political claim about the
class character of the three state wings (finance maintains the imperial circuit, security its
repressive capacity, the settler wing the border — `npc_stub.py:471-479`). Confirming it as
standing mechanics rather than stopgap is the Director's call. The bonus coefficient is Θ-feel,
already ruled; not re-asked.

---

## Row 19 — Consciousness @17.0: three `national_identity: 0.5` absent-data defaults

**Register row (verbatim §5b):** `| 19 | Consciousness @17.0 | `"national_identity": 0.5`
absent-data default, **three copies** (`ideology.py:74,82,89`) — a class with no ideology record
is half-nationalist | **Unflagged by the reader.** Must be re-declared explicitly as a
`deffield` default and routed past the Director, not inherited silently |`

**Code evidence (verified — register's line numbers exact):**

- `_get_ideology_profile_from_node`, `src/babylon/engine/systems/ideology.py:57-91`: copy 1 at
  **74** (no `ideology` attr), copy 2 at **82** (dict `.get` fallback), copy 3 at **89** (unknown
  format). Consumers: 353, 359 (the consciousness router); fascist drift then accumulates from
  the fabricated base at 411 (`new_nation = min(1.0, current + delta_f)`).
- Semantic: `ideology.py:99` — "national_identity: Relationship to State/Tribe
  [0=Internationalist, 1=Fascist]". 0.5 is not neutral; it seats an unrecorded class halfway to
  the fascist pole on day one.
- No `defines.yaml` entry exists for this default (grep-verified) — an inline magic constant,
  exactly "inherited silently".
- Cross-row: two more copies in the struggle stack (`struggle.py:128, 137`) — row 17's surface;
  sweeping only `ideology.py` leaves the constant twice in the ported estate.
- Position `17.0`: `ideology.py:109`.

**Binding rulings:**

- Constitution III.11 (loud failure / absence over fabrication) — the default is
  fabrication-in-absence by construction.
- ADR016 (direction law) — the router consuming this value is the ratified bifurcation
  mechanism; ADR202 R7 left ADR016 untouched while reversing the sibling Gaussian site.
- ADR171: Phase-3 consciousness wiring was withdrawn and re-filed as OQ5+OQ9, the
  oppressed-nation guard ordered BEFORE any consciousness wiring; a silent default is exactly
  the wiring ADR171 refused to improvise. Its process precedent: open-question defaults "taken
  … conservative, Director-overridable".
- ADR202 R1's NoDataSentinel is the standing absence pattern on the ladder estate.

**Options:**

- **A. Re-declare as an explicit `deffield` at 0.5.** The register's ask, form-wise: visible,
  Director-routed, byte-identical. But it affirms the political content (unrecorded ⟹
  half-nationalist) — the affirmation is the ruling, not the paperwork.
- **B. Re-declare as a `deffield` at another value** (e.g. 0.0). Equally explicit, equally a
  political claim in the opposite direction. Director-only either way.
- **C. Absence, not default.** Unrecorded classes carry NO `national_identity` reading;
  consumers gate on presence (NoDataSentinel pattern). Most III.11-consistent; baseline-moving
  vs the frozen engine — declared §6.5 ceremony plus router rework.
- **D. Fold the value question into the ADR171 OQ5/OQ9 session.** The already-chartered venue
  for consciousness×national-question wiring; meanwhile the port cannot transcribe the constant
  at all (blocking).

**Workforce recommendation: A or C — her pick; the form is non-negotiable.** Silent inheritance
is the one unacceptable branch. A preserves byte-identity under explicit declaration; C is the
constitutionally cleaner end-state and matches the ladder estate's absence pattern, at the cost
of a baseline ceremony and router rework. Workforce mildly prefers **A at port time with C
recorded as the named follow-up**, since ADR171 reserves the consciousness×national-question
wiring to its own session — but the VALUE is hers, not ours. Either way, sweep or explicitly
exempt the two `struggle.py` copies.

**Reserved-line flags (Director-only):** the value of an absent-data default with political
content — what ideology an unrecorded class is presumed to hold — is ideological taxonomy by
another name. Only the Director rules it.

---

## Row 20 — ReserveArmy @5.0: the border-valve throttle ("the settler-wing wage bargain")

**Register row (verbatim §5b):** `| 20 | ReserveArmy @5.0 | The border-valve throttle — "the
settler-wing wage bargain" (`reserve_army.py:86-88`) | **Unflagged by the reader.** Which wing
the bribe protects, and whether the valve exists at all, is not a port-time call (ADR171) |`

**Code evidence (verified — register's line numbers exact):**

- Throttle: `src/babylon/engine/systems/reserve_army.py:83-91` — overlay present ⟹ `border =
  self._border_valve(...)` (84); comment at **86-88**: "A tighter border throttles the reserve
  army's replenishment: the effective ratio shrinks, wage pressure eases — the settler-wing wage
  bargain." Code: `reserve_ratio *= 1.0 - border` (89).
- Valve read: `_border_valve`, `reserve_army.py:123-146` — the territory's TOP claims-holder's
  `border_regime` overlay magnitude; absent ⟹ 0.0 ("an open valve, the pre-U9 behavior", 133).
- Provenance: `reserve_army.py:62-67` — overlay written by PolicySystem @17.47 the prior tick
  (P25 U9, ADR135; I-ORD grain); "Absent register ⟹ identical math — the qa six never carry it"
  (65-66). `PolicyAxis.BORDER_REGIME` = "reserve-army inflow valve",
  `src/babylon/models/enums/politics.py:46` (doc 27).
- Port note: inert on every qa golden (no overlay register) — verbatim port is byte-safe by
  construction.

**Binding rulings:**

- ADR135 §6: `border_regime → ReserveArmySystem effective-ratio valve` is a **declared
  read-side**, landed with its producer — the valve's existence is ratified wiring.
- ADR171: the national-question estate — the bribe channel (Ω̂) and the settler premium are the
  Director's ruled line; "which wing the bribe protects" sits inside it.
- ADR202 R5 (C8): the wage-pressure CURVE is ruled Option A (full ladder at port; NO wage floor
  — "super-exploitation stays expressible"). That ruling covers `reserve_army/calculator.py`,
  not this overlay coupling; both belong to the same ReserveArmy port train.
- ADR188 Row 7 / ADR202 R0: curve estate stands; nothing here is a curve.

**Options:**

- **A. Port-as-is, plain.** Verbatim; inert without overlays. Risk: transcribes the name and the
  politics without routing the question — the failure the register flags.
- **B. Port-as-is under a D-record (recommended).** Verbatim (ADR135-ratified, byte-safe);
  D-record names the ideological content — border enforcement modeled as a wage-protection lever
  for the settler wing — and parks the existence/beneficiary question in the ADR171 estate for
  the next national-question session.
- **C. Do not port the valve now.** U9's declared read-side goes consumerless — the
  honest-partial pattern register row 23 interrogates; permissible only as a declared partial,
  and it orphans ADR135 wiring.
- **D. Retire the valve.** Rules that border enforcement does not protect settler-wing wages in
  the model. Director-only; contradicts ADR135. Listed because "whether the valve exists at
  all" admits it.

**Workforce recommendation: B.** Existence is ratified wiring (ADR135); the line question — the
name, the beneficiary, the bargain — is ADR171 terrain. A D-record carries both facts without
silent inheritance or scope shrink.

**Reserved-line flags (Director-only):** "the settler-wing wage bargain" — whether the valve
exists, and which wing the bribe protects. National-question line (ADR171); the workforce
transcribes, the Director rules.

---

## Cross-row notes

- Rows 17+19 share a constant: the `0.5` default lives five times across two systems
  (`ideology.py:74,82,89`; `struggle.py:128,137`). One ruling should dispose all five copies.
- Rows 18+20 are coupled: `SETTLER_POPULIST→border_regime` (row 18) produces the overlay row
  20's valve consumes. Ruling 18-C or 20-C independently breaks the chain; paired B/B keeps it
  intact.
- All four recommendations are transcription-plus-paperwork; none re-opens a landed ruling. New
  content requested of the Director: the row-19 default VALUE, the row-17 taxonomy-minting
  ceremony, the row-20 bargain.
