# Director-gate register memos — rows 21–24 (issue #564, async ruling)

**Process.** ADR202 R11 ruled rows 9–24 memos-first, async-ruled; this memo covers rows 21–24 (the
§5b tail + all of §5c). Verbatim row text from #564 (source:
`reports/port-estate-survey-2026-08-12.md:325,331-333`). Every anchor re-verified against the tree
2026-08-14; stale citations corrected inline. Nothing here rules anything — each row ends at
lettered options, a workforce recommendation, and the flags that are the Director's alone.

**Post-survey movement bearing on all four rows.** T2 landed (ADR201: `edges`/`edge-between`/
`field-of` over an EdgeRef are SERVED; `GraphSubstrate::edge_attribute` exists at
`rust/crates/babylon-graph/src/substrate.rs:271`, strength-only until T3). T3 (ADR198 R1–R3
edge-attribute storage, #560) is chartered. T6 landed (#563: six memos in `reports/t6-dormancy/` +
synthesis `reports/t6-tickdynamics-services-charter-2026-08-14.md`). ADR202 discharged curve rows
1–8. **Staleness corrections:** the adjudications' `evaluator.rs:503-512` anchors moved —
`UNSERVED_EXPRESSION_HEADS` is now `evaluator.rs:523-530` (`the` :524, `membership-field-of` :529);
`edges`/`edge-between` left it (`SERVED_QUERY_HEADS` :546, arm :579); "substrate.rs has no
edge-attribute accessor" is post-T2 stale. `DECLARABLE_INTRINSICS` still `["exp","log","floor"]`
(`declarations.rs:110`) — `sqrt` absent.

---

## Row 21 — TickDynamics: bifurcation score + ClassDistribution + cascade milestones

**Verbatim:** "21. TickDynamics bifurcation-risk score + ClassDistribution + cascade milestones
(rides #563's re-read)". Survey: "Unflagged by the reader on the ideologically densest system in
the batch."

**Surface.** TickDynamicsSystem @4.0 (`src/babylon/domain/economics/tick/system/__init__.py:112`) —
the largest unported Material-Base surface; the only inventory flagged "a re-read is REQUIRED"
(survey §6). The re-read is delivered (#563).

**Code evidence (verified).**
- **Bifurcation directional score** — semantics −1 revolutionary / +1 fascist; formula
  `raw = −w_s·solidarity + w_b·burden`, dampened `(1−legitimation)`, clamp [−1,+1]
  (`crisis/bifurcation.py:9-17`); defines `config/defines/economy_basic.py:113-134` — FOUR reads,
  not the register's three: `bifurcation_solidarity_weight`/`bifurcation_burden_weight`/
  `class_burden_epsilon` (constructor, `system/__init__.py:2269-2275`) plus
  `bifurcation_event_threshold=0.5` (gate :2277, :2294-2302); direction string :2329; half-even
  payload rounding :2336-2340. Dormancy (T6): `compute()` never fires in any qa scenario (sole
  boundary has empty prev states, :2281-2284); in michigan-e2e it fires from the 2nd boundary but
  is structurally NEUTRAL while crisis is NORMAL (`bifurcation.py:93-94`), and SOLIDARITY is
  deliberately never seeded (`bridge.py:842-846`, III.5/Q4) — the revolutionary (−) term risks
  structural zeroing while the fascist (+) burden term is not. The Feature-030 legitimation blend
  is unreachable (`node.id == fips`, `bifurcation.py:101-104`).
- **Five-share ClassDistribution + Feature-016** — model `dynamics/types.py:27-70` (bourgeoisie and
  petit-bourgeoisie externally fixed; the engine operates on LA/proletariat/lumpen); bootstrap
  shares 0.01/0.09/0.40/0.35/0.15 (`system/__init__.py:822-830`); transition engine
  `dynamics/transition_engine.py:107-186`, gated `transition_engine is None → return` (:2366-2367).
  Live only in michigan-e2e (83 counties × 9 boundaries, :2424-2454); frozen in all qa scenarios.
- **`crisis.dispossession_cascade_milestones`** — `[0.05, 0.10, 0.15]` (`economy_basic.py:137-140`);
  sole reader `system/__init__.py:1147` (`_check_dispossession_cascade`, :1115-1170), reachable
  only from :2444-2452 (transition engine + crisis ≠ NORMAL + prev states). DISPOSSESSION_CASCADE
  emitted in zero committed artifacts.

**Existing rulings that bind.** ADR016 (the George Jackson direction law — the score's semantics ARE
its metric form); ADR070/Program 19 (emergent class partition — the taxonomy coexists with that
cutover); ADR171 (the LA share is the theory's central stratum); ADR198 R7 (FIPS-keyed state ports
int-encoded); ADR202 R5 (this port's wage-pressure curve — full ladder, no stub); III.5/Q4.

**Options.**
- **A. Port-as-is on all three** — verbatim content (defconsts/D-records) at the port. Cheapest;
  but ratifies three theory-laden surfaces the inventory never flagged, by silence.
- **B. Confirm the taxonomy, re-specify the score** — ClassDistribution verbatim; the score gets a
  theory note first (its revolutionary term is structurally disarmed under III.5/Q4 — confirm
  intended, not a seeding artifact); milestones ride with it (same FR-022 machinery).
- **C. Full dossier treatment** — all three through the T4-style dossier pattern first. Most
  rigorous; delays the largest Material-Base port behind a new document.
- **D. Defer to the port charter** — bundle into the TickDynamics port packet with T6's open
  Director residue (`employment_source`, synthesis §5.1).

**Workforce recommendation: B.** The five-share taxonomy is the frozen engine's core MLM-TW
structure, golden-pinned and heavily validated — verbatim is right. The score is different in kind:
it encodes ADR016's law, was never flagged by its own inventory, and the T6 re-read proved its
revolutionary half cannot fire under standing no-SOLIDARITY seeding — porting it verbatim
unexamined encodes a one-directional bifurcation by accident.

**Reserved-line flags (Director-only).** The −1/+1 direction semantics (ADR016 law). Whether the
0.40 LA bootstrap share and the 5/10/15pp milestones are the intended pedagogy of dispossession.
Whether the revolutionary term's structural zeroing is a feature (crisis direction is earned by
organizing) or a defect.

---

## Row 22 — Slice-4 first consumer (Allegiance/Electoral)

**Verbatim:** "22. Slice-4 first consumer (Allegiance/Electoral) — **the dyadic half is RULED,
ADR198 R1-R3 (#560)**; any hyperedge-membership half rides R4/#536. Residue: confirm which half
Allegiance/Electoral actually need after T3 lands"

**Surface.** AllegianceSystem @17.42 + ElectoralSystem @17.45 (survey C-3: "neither ports at all
until the open-cardinality `allegiance` map has storage").

**Code evidence — what they actually read today (verified).**
- The load-bearing channel is **node-local dict state**, not edge-shaped:
  `SocialClass.allegiance: dict[str, float]`, keyed by party org ids (`social_class.py:323-333`).
  Written each tick by AllegianceSystem (`allegiance.py:164-172`, `{"allegiance": …, "hope": …}`);
  read SAME-TICK by ElectoralSystem: `allegiance` at `electoral.py:484-485, 644-645, 709-710,
  732-738, 981`; `hope` at :711, :716 (the turnout law). `hope` is transient (`world_state.py:86-90`).
- MEMBERSHIP edges exist but are **bare presence sets**: `electoral_fixture.py:80-86` (party→class,
  no payload); Allegiance uses them only for contact 0/1 (`allegiance.py:261-264`, :400). The
  allegiance payload does not ride edges in the frozen engine.
- Electoral's other edge need: C2's TRANSACTIONAL write wants TWO named fields on one edge
  (`edge_mode` + `co_optive_dependence`) — exactly the R1–R3 dyadic storage T3 implements.
- Registers: class/sovereign-keyed ones decompose onto ordinary nodes (landed Slice 1); only true
  singletons need `the` (T2.5, #572 — open per ADR202 R10). Language table: `membership-field-of`
  is the only slice-4 head (`evaluator.rs:529`), "Director-ruled deferred to first consumer".

**Residue finding (the asked confirmation).** On the adjudicated record the half they need is the
**membership half**: both inventories name Amendment AG attributed membership as the allegiance
map's ratified destination, and ADR198 R4 assigned AG(i) payloads to Community's #536 ceremony. The
dyadic half (T3) serves exactly one sub-computation (Electoral C2). Post-T3 an **unadjudicated
alternative** exists: parties port as ordinary NodeTypes (`organization-foundation.bscn`
precedent), so the allegiance map is node→node and could ride T3 storage alone — a new ALLEGIANCE
edge type with one float field, seeded per class×party pair at scenario load (runtime `add-edge`
is Task-12 load-gated). Tension to flag: ADR189 REJECTED the dyadic-edge landing for
(member, hyperedge) payloads as unwinding Amendment D by the back door; that rejection was scoped
to hyperedge incidence, but its spirit is one ruling away from this encoding.

**Existing rulings that bind.** ADR197 (slice 4 "DEFERRED TO ITS FIRST CONSUMER"); ADR198 R1–R3
(dyadic storage → T3) and R4 (AG(i) → #536; "two save-compat steps accepted over one"); ADR189/
Amendment AG (the design + the dyadic rejection); ADR201 (T2 landed); ADR202 R4 (Allegiance's
sigmoid already ruled — band measure) and R10; ADR134 (the valve).

**Options.**
- **A. Charter slice 4 now, first consumer named** — pull the AG(i) ceremony out of #536 (or
  co-sponsor it with Community's design input). Unblocks the cluster on the ratified design; costs
  R4's sequencing.
- **B. Re-sequence behind #536** — both systems wait for Community's ceremony as R4 ruled. Keeps
  one ceremony; delays the estate's largest event emitter (Electoral, 10 events) and five electoral
  goldens behind Community's design work.
- **C. Rule the dyadic encoding** — allegiance map as a T3 edge attribute, no slice-4 ceremony for
  this estate. Fewest lanes; but walks up to ADR189's rejected alternative on a reserved-adjacent
  surface — Director-only territory.
- **D. Split ruling** — confirm T3 for C2 (already ruled; no ceremony), defer the allegiance-map
  home to a one-page modeling note comparing AG(i) vs dyadic before either is built. Slowest to
  code; cheapest to reverse.

**Workforce recommendation: A, with D as the acceptable fallback.** The residue answer by evidence:
they need **both** halves — the membership half for the load-bearing allegiance/hope channel, the
dyadic half for C2 — so re-sequencing buys nothing once T3 lands. ADR197's deferral was explicitly
"to its first consumer"; the consumer is here, named, with goldens waiting. If the Director wants
the dyadic alternative priced before committing CanonicalState to AG(i) payloads, D scopes that to
one document instead of a ceremony.

**Reserved-line flags.** Whether the electoral allegiance mass (an ideological quantity — who a
class believes serves it) is AG(i)'s first payload is a framing call. The dyadic-vs-AG(i) choice
touches Amendment D/AG's settled shape — amendment territory, not a port D-record. Any change to
what `allegiance` MEANS (the valve's H(c) semantics, ADR134) is the Director's line.

---

## Row 23 — Honest-partial packs (ContradictionField+FieldDerivative, Policy, MarketScissors)

**Verbatim:** "23. Honest-partial packs (ContradictionField+FieldDerivative, Policy,
MarketScissors) — does Territory's no-sliver precedent bind, or is a carrier pilot worth a
partial?"

**The precedent (verified).** `reports/territory-port-phase1-inventory-2026-08-11.md:125-149`: the
sliver (heat + camp decay) was "rejected as silent scope shrink" (:131-134) because "the pipeline
is one behavior" (:129-131) — "Territory then ports once, completely, per the no-MVP standing
feedback" (:148-149).

**Code evidence (verified).**
- **ContradictionField @19.0** — sole surviving blocker: Computation 2's per-edge `tension` read
  (edge-attribute storage = T3). Atomization, the node-iteration shell, the clamp and the 3-slot
  history are PORTABLE WITH D-RECORDS on landed Slice 1 via a `:ceiling 1` carrier; outputs
  byte-gated via `_restamp_field_stack` (`world_state.py:855-867`). Caveat: the field stack is
  provably two fields; an atomization-only pack narrows FieldDerivative's input domain — the
  partial's ported half is NOT behaviorally invariant.
- **FieldDerivative @20.0** — only Phase 1 (`field_gradients`, edge storage read+write = T3)
  blocked; Phases 3/4 portable with D-records (Option-representation record for `field_name`);
  Phase 4 NOT-A-PACK (plumbing).
- **Policy @17.47** — resolver math + 4 per-entity registers portable; post-T2 the Slice-2 residue
  is: `_org_bridges` edge-attribute read (T3), `national_financial` via `the` (T2.5/#572),
  incumbent read-back (served). **Zero byte-gate coverage** (registers are graph metadata;
  `tick_capital_stock` prefix-filtered at `world_state.py:256`) — oracle is behavioral
  (`test_electoral_goldens.py`). Its portable half (resolver math) IS invariant to its blocked half.
- **MarketScissors @17.8** — national oscillator core portable today on a carrier; blocked on the
  per-county axis (`county_fips` string — ADR198 R7's int-encoding now rules that shape), the
  `price_divergence` tri-state (no Option representation), nested TickDynamics reads (producer
  unported), MARKET_CORRECTION (WS1). Its curve is RULED (ADR202 R8: per-county Carrier-B ensemble,
  `scissors_balance_scale` retired, §6.5 ceremony expected). The survey's "behind D35/D65" caveat
  has shifted: that storage is now RULED and chartered as T3 (#560) — the wait has a train.

**Existing rulings that bind.** The Territory DEFER precedent; ADR198 R6 (carrier blessed as the
graph-scope standard) + R8 (T1 = carrier-pattern doc train); ADR202 R8; the port-as-is standing
ruling.

**Options.**
- **A. The precedent binds, full stop** — no partials; each system ports once, complete, after its
  blocker trains (T3 for C-1; T2.5+T3 for Policy; R7-encoding + TickDynamics for MarketScissors).
  Honors no-MVP; the carrier pattern stays unproven in code until then.
- **B. Sanction the declared-partial carrier pilot** — ship C-1's portable halves now, blocked edge
  halves named in the pack header as declared scope. Proves the pattern for ~8 inheriting systems;
  honest cost: the C-1 partial diverges from frozen behavior (narrowed field stack) → goldens move
  → a §6.5 ceremony plus a declared-divergence window until T3 completes the packs.
- **C. C-1 behind T3, first consumer, complete** — the pilot ships whole as T3's first port
  consumer; carrier proof and no-sliver law both honored; the proof waits for #560.
- **D. Prove the pattern without a system port** — T1's carrier doc + a synthetic `.bscn` exercise
  (the Task-15 precedent: Territory's shapes were proved on a synthetic fixture before its port).
  Zero golden risk; packs still wait for blockers.

**Workforce recommendation: D, then C.** The Territory rule is not anti-partial in the abstract —
it refuses spending a train on a sliver whose blocked half changes the ported half's behavior.
Applied here: Policy's portable half is invariant (strongest partial case) but byte-gate-invisible,
so a partial lands unverifiable by `qa:regression`; C-1's portable half is NOT invariant;
MarketScissors' curve ruling already schedules a golden-moving ceremony — fold the port into it.
Prove the carrier pattern on a synthetic fixture (D), then run C-1 complete as T3's first consumer
(C). If the Director judges the pattern proof urgent enough to accept a declared-divergence window
and a ceremony, B is the fast path — but that is exactly the trade the Territory session refused.

**Reserved-line flags.** Whether "port once, completely" is LAW (binds every pack) or HEURISTIC
(waivable per pack by the Director) — that is the actual row-23 question and only she can answer
it. Any golden drift is §6.5 ceremony territory. The scissors ensemble's content shape (R8) is
ruled and not re-opened here.

---

## Row 24 — Shadow/dead outputs (field_registry + five dead outputs)

**Verbatim:** "24. Shadow/dead outputs (field_registry never wired; five verified dead outputs) —
port verbatim under port-as-is, or retire on the WS4 ledger?"

**Code evidence (every anchor re-verified 2026-08-14).**
- **`field_registry`** — protocol declaration `kernel/services.py:43`; default
  `engine/services.py:196` (`field(default=None)`); consumers `contradiction_field.py:86` and
  `field_derivative.py:70`, both falling back to the E0 opposition-source path when None;
  corroborated at `world_state.py:70` and `sentinels/seam/registry.py:1584,1684`. Zero non-test
  assignment sites (rg over `src/` + `web/`). Feature-002's registry path is dead in production.
- **`transport_demand_signal`** — writer `transport.py:168`; transient-declared
  (`world_state.py:152`); ZERO readers (transport inventory :158, :317). The system is default-OFF;
  the intended consumer (sovereign OODA budget, ADR165 item 2 — `defines.yaml:1144` comment) was
  never wired. Hash-invisible (never written on canonical).
- **`balkanization.effective_controller_by_territory`** — writer `sovereignty.py:127`
  (persistent_data channel); zero readers; never on the graph → hash-invisible.
- **`sigma_capital_labor` / `sigma_wage` / `derived_class_cell`** — writer `contradiction.py:950-966`
  (`_write_pole_shadow`; Program 19 Phase-1 shadow, ADR070); transient (`world_state.py:83-85` —
  dropped on reconstruction, NOT restamped into `to_graph`); zero readers. Hash-invisible.
- **`wealth_share`** — declared `SocialClass` field (`social_class.py:312`); writer
  `wealth_distribution.py:274`; zero readers in engine/projection/intelligence (wealth-distribution
  inventory :337-344, :495-498). Its own description: "nothing feeds back from it yet (Phase 2 is
  owner-gated)" (`social_class.py:315-321`). **The only hash-VISIBLE one of the five** (declared
  field → `to_graph` → `graph_content_hash`, `tools/regression_test.py:924-964`).
- **`_swell_reserve_army`** — `market_scissors.py:439-460`, called :385; inert by naming mismatch:
  gates on territory `median_wage` (`territory.py:216`, default 0.0) no factory ever sets
  non-zero; the production wage write is the deliberately distinct `tick_median_wage`
  (`sentinels/seam/registry.py:783-784`). Never fires on the canonical estate → hash-neutral today.
- **Dependency:** the T6 charter already banks on this row — "Dead-ends port verbatim or not at
  all … retired on the WS4 ledger per register row 24's ruling — no third option"
  (`reports/t6-tickdynamics-services-charter-2026-08-14.md` §4.4).

**Existing rulings that bind.** The port-as-is standing ruling; the WS4 dead-code adjudication
ledger (Territory inventory's AUTO-mode row: "bless as reserved, or retire"); ADR070 (the sigma/cell
outputs are Program 19's instrumentation); ADR165 item 2 (the transport signal's design intent);
Program 21 Phase 1→2 (wealth_share's planned feedback); §6.5 (hash-moving retirement = ceremony).

**Options.**
- **A. Port verbatim, all of it** — maximal fidelity to the frozen reference; spends port effort on
  values nothing reads and carries dead weight into BSL content to be maintained (or retired later
  anyway).
- **B. Retire everything on WS4 now** — five of six retirements are hash-neutral today;
  `wealth_share` moves goldens → one §6.5 ceremony. Cleanest target estate; but deletes Program
  19/21 shadow instrumentation whose feedback phases are owner-gated plans, and amends ADR165's
  intent — three reserved surfaces in one row.
- **C. Split by visibility and reservation** — retire the hash-invisible, unreserved four now
  (field_registry branches, `transport_demand_signal`, `effective_controller_by_territory`,
  `_swell_reserve_army`); carry `sigma_*`/`derived_class_cell` and `wealth_share` as documented
  dormancy until Program 19's cutover and Program 21 Phase 2 rule on them. One small WS4 ceremony,
  zero golden drift, zero reserved-line trespass.
- **D. Defer the whole row to WS4** — each port train carries its dead outputs as documented
  dormancy until the sweep. Blocks no train (the T6 charter runs on "verbatim OR retired"); leaves
  dead code accumulating across every Wave.

**Workforce recommendation: C.** The only option that is hash-neutral, reversible, and
reservation-clean at once. The four unreserved dead outputs have verified zero readers and zero
golden presence — retiring them is hygiene, not theory. The two reserved-adjacent ones
(`sigma_*`/`derived_class_cell` = Program 19 instrumentation; `wealth_share` = Program 21 Phase-2's
planned input) belong to the programs that own them, not to a port-efficiency row.

**Reserved-line flags.** `sigma_capital_labor`/`derived_class_cell` instrument the emergent-class
partition shadow (ADR070) — deleting them pre-empts a Program 19 call. `wealth_share`'s "Phase 2 is
owner-gated" is on its face a reserved plan — retiring forecloses it. `transport_demand_signal`'s
consumer is ADR165's design — retiring amends it. And the general law — whether port-as-is extends
to provably dead code — is the Director's to state, not the workforce's to infer.
