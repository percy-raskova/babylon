"""The Intelligence — AI narration and semantic retrieval (Program 14).

The Embedded Trinity's third pillar as a package: ``ai/`` (director,
narrative, personas — the Observer that narrates state changes) and
``rag/`` (chunking/embedding retrieval over the Archive). Constitution
II.5: the AI observes and narrates; the engine adjudicates the math —
nothing in this package may mutate simulation state.
"""
