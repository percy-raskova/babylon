{#-
Briefing dossier page skeleton (Program 24 P2 WO-35; Constitution III.13).

NET-NEW page: no design-brief S-item names a briefing dossier — the design
canon's own governance list (§9's item 4) flags a DESIGN_BIBLE "wiki-page
anatomy" section as not yet written. This mirrors the ratified county page
anatomy (frontmatter + fenced {statblock}); DESIGN_BIBLE §9b's crimson/gold
newt-plate chrome is a TUI-widget-layer concern for a later fidelity pass —
a markdown scaffold carries no CSS of its own.

Deliberately prose-light: every value here is a plain string already
resolved by babylon.projection.vault.render_briefing (see that module's
docstring — a BriefingView has no honestly-absent field, so unlike
county.md.j2 there is no {absence} block on this page).

Fenced directives only ({statblock}), matching babylon.tui.directives.
BabylonFence dispatch (ADR099: paired containers break the 8.2.8 markdown
walker).
-#}
---
id: briefing/{{ briefing.session_id }}
name: Operation {{ briefing.codename }}
verified_tick: {{ briefing.verified_tick }}
staleness: verified as of tick {{ briefing.verified_tick }} — always regenerable, never authoritative
---

# OPERATION {{ briefing.codename }} — Scenario Briefing

```{statblock} briefing/{{ briefing.session_id }}
{% for label, value in statblock_rows -%}
{{ label }}: {{ value }}
{% endfor -%}
```

## The Stakes

The campaign runs {{ briefing.horizon_years }} years — {{ briefing.horizon_ticks }} weekly turns. Nothing ends early: as material conditions move, the world settles toward one of five recognized patterns. The campaign closes at the horizon, or when a locked pattern is accepted early.

## Five Ways the Century Can Land

{% for objective in objective_rows %}
### {{ objective.title }}{% if objective.win_badge %} — {{ objective.win_badge }}{% endif %}

{{ objective.description }}

- id: `{{ objective.id }}`
- status: {{ objective.status }}
- current reading: {{ objective.progress }}
{% endfor %}
