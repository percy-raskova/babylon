{#-
County dossier page skeleton (Constitution III.13 / Amendment W).

Deliberately prose-free: this is a scaffold page, not narrative copy. Every
value interpolated here is a plain string already resolved by
babylon.projection.vault.render (never a raw Optional CountyView field —
Jinja's StrictUndefined only fires on genuinely *absent* names, and a
present-but-None field renders the literal text "None" rather than raising,
so render.py precomputes statblock_rows/absent_fields and the template only
ever touches those and the three always-present identity fields).

Fenced directives only ({statblock}/{absence}), matching
babylon.tui.directives.BabylonFence dispatch (ADR099: paired containers
break the 8.2.8 markdown walker).
-#}
---
id: county/{{ county.county_fips }}
name: County {{ county.county_fips }}
verified_tick: {{ verified_tick }}
staleness: verified as of tick {{ verified_tick }} — always regenerable, never authoritative
---

# county/{{ county.county_fips }} — Dossier

```{statblock} county/{{ county.county_fips }}
{% for label, value in statblock_rows -%}
{{ label }}: {{ value }}
{% endfor -%}
```

## Sovereignty

{% if county.sovereign_id is not none -%}
Claimed by [[sovereign/{{ county.sovereign_id }}]] via a CLAIMS edge.
{% endif %}
{% for field, remedy in absent_fields -%}
```{absence} {{ field }} — {{ remedy }}
```
{% endfor %}
