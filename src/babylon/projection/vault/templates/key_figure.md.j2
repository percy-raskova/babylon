{#-
Key-figure dossier page skeleton (Constitution III.13 / Amendment W).

UNCONDITIONALLY the honest-absence page (Program 24 P2 WO-21): ADR084
formally retired the backing KeyFigure model + WorldState.key_figures
(ai/decisions/ADR084_retire_dead_models.yaml), and NodeType.KEY_FIGURE was
reclassified declared-but-not-production-stamped with no
MODEL_FIELDS_BY_NODE_TYPE entry left -- there is no field this dossier could
ever attribute, for any id, in this engine version.

No {statblock} fence is emitted here, deliberately: the keel's own P1 exit
reconciliation made an empty statblock fence body the LIVE-page signal ("the
provider path serves only empty-body live pages" -- commit 0b9c9f64), and
this baked page is never live -- a fence that could never carry a row would
only invite that ambiguity, which is WO-29's territory, not this one's. The
single {absence} block below is the whole story.

Fenced directives only ({absence}), matching
babylon.tui.directives.BabylonFence dispatch (ADR099: paired containers
break the 8.2.8 markdown walker).
-#}
---
id: key_figure/{{ key_figure.key_figure_id }}
name: Key Figure {{ key_figure.key_figure_id }}
verified_tick: {{ verified_tick }}
staleness: verified as of tick {{ verified_tick }} — always regenerable, never authoritative
---

# key_figure/{{ key_figure.key_figure_id }} — Dossier

```{absence} key_figure — {{ dead_producer_remedy }}
```
