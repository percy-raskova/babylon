{#-
National dossier page skeleton (Constitution III.13 / Amendment W).

Deliberately prose-free: this is a scaffold page, not narrative copy. Every
value interpolated here is a plain string already resolved by
babylon.projection.vault.render_national (never a raw Optional NationalView
field — Jinja's StrictUndefined only fires on genuinely *absent* names, and a
present-but-None field renders the literal text "None" rather than raising,
so render_national.py precomputes statblock_rows/absent_fields and the
template only ever touches those and the always-present identity fields:
national_id, verified_tick, imperial_rent_pool — see NationalView's docstring
for why the Gas Tank stock is never withheld).

Fenced directives only ({statblock}/{absence}), matching
babylon.tui.directives.BabylonFence dispatch (ADR099: paired containers
break the 8.2.8 markdown walker).
-#}
---
id: national/{{ national.national_id }}
name: National {{ national.national_id }}
verified_tick: {{ verified_tick }}
staleness: verified as of tick {{ verified_tick }} — always regenerable, never authoritative
---

# national/{{ national.national_id }} — Dossier

imperial_rent_pool: {{ imperial_rent_pool }}

```{statblock} national/{{ national.national_id }}
{% for label, value in statblock_rows -%}
{{ label }}: {{ value }}
{% endfor -%}
```

## Sovereignty

{% if national.sovereign_id is not none -%}
Claimed by [[sovereign/{{ national.sovereign_id }}]] via a CLAIMS edge over every territory nationwide.
{% endif %}
{% for field, remedy in absent_fields -%}
```{absence} {{ field }} — {{ remedy }}
```
{% endfor %}
