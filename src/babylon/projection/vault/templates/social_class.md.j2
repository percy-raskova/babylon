{#-
Social-class dossier page skeleton (Constitution III.13 / Amendment W).

Mirrors county.md.j2's discipline exactly (see that template's header
comment for the full rationale): deliberately prose-free, every value here
is a plain string already resolved by
babylon.projection.vault.render_social_class (never a raw Optional
SocialClassView field — StrictUndefined only fires on genuinely *absent*
names, so render_social_class precomputes statblock_rows/absent_fields and
this template only ever touches those plus the three always-present
identity fields).

Fenced directives only ({statblock}/{absence}), matching
babylon.tui.directives.BabylonFence dispatch (ADR099).
-#}
---
id: social_class/{{ social_class.class_id }}
name: Social Class {{ social_class.class_id }}
verified_tick: {{ verified_tick }}
staleness: verified as of tick {{ verified_tick }} — always regenerable, never authoritative
---

# social_class/{{ social_class.class_id }} — Dossier

```{statblock} social_class/{{ social_class.class_id }}
{% for label, value in statblock_rows -%}
{{ label }}: {{ value }}
{% endfor -%}
```

## County

{% if social_class.county_fips -%}
Attributed to [[county/{{ social_class.county_fips }}]].
{% endif %}

{% for field, remedy in absent_fields -%}
```{absence} {{ field }} — {{ remedy }}
```
{% endfor %}
