"""Topology surface renderers for The Archive TUI (Lane T: PAOH/egotree/matrix).

Deliberately minimal — each Lane T work order (WO-30 PAOH, WO-31 Levi/egotree,
WO-32 incidence/adjacency matrix) adds its own module here and nothing else,
so this file stays a merge-safe no-op across sibling worktrees.
"""

from __future__ import annotations
