/**
 * MSW request handlers — empty for the scaffold (spec-110 B1).
 *
 * B2 adds real handlers as API-backed features land; keeping this empty and
 * explicit avoids inventing response shapes ahead of the endpoints they mock.
 */

import type { HttpHandler } from "msw";

export const handlers: HttpHandler[] = [];
