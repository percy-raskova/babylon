"""Unit tests for the ``babylon.engine.optimization`` package.

These are diagnostic-tool tests (not gated by ``mise run check``) — see
``tests/unit/engine/optimization/*`` docstrings and ``.mise.toml``'s
``test:optimization`` task.
"""
