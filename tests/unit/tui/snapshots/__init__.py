"""Snapshot-test package for ``babylon.tui`` widgets (Program 24 P2 Wave 1).

Colocates each Lane W/T widget's ``pytest-textual-snapshot`` golden with its
launcher module, kept separate from the plain (non-visual) contract tests
directly under ``tests/unit/tui/``.
"""
