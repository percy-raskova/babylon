"""Per-kind Lane P page snapshot tests (Program 24 P2).

Distinct from the top-level ``tests/unit/tui/test_snapshot.py`` (the P1 keel's
single county-sample-page golden): each Lane P WO gets its own dedicated
launcher + snapshot test here, avoiding any collision with the keel file or
with sibling Lane P WOs' own additions.
"""
