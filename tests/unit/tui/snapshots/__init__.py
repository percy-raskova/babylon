"""Per-kind/per-content-lane TUI snapshot tests (Program 24 P2).

Colocates each snapshot golden with its launcher (``pytest-textual-snapshot``
resolves the launcher path relative to the calling test file) — a new
subdirectory as the fan-out lanes land pages beyond the P1 keel's single
county dossier.
"""
