"""Per-widget golden snapshot tests for the Archive terminal client.

One subpackage per Lane P/W/T deliverable's own snapshot test + launcher
(WO-16 onward), distinct from the whole-shell golden in
``tests/unit/tui/test_snapshot.py``.
"""
