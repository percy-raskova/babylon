"""Developer observability and frozen-reference analysis tools.

The top-level diagnostics stay standard-library-only. The nested
``sim_analysis`` suite uses dependencies from the development dependency group
and is deliberately outside the installable ``babylon`` package.
"""
