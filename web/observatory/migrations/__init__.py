# The Observatory app declares NO models and therefore contributes NO
# migrations. This empty package exists so Django recognises the app as
# migration-capable (and so `makemigrations --check` confirms zero models).
